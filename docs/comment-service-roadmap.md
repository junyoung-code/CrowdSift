# 댓글 관리 서비스 개발 로드맵

> 이 문서가 분류 기준의 원본이다. 기준이 바뀌면 여기를 고치고, 같은 커밋에서 코드와
> 프롬프트를 맞춘다. 노션에는 이 문서를 옮겨 공유한다.

## 현재 집중 단계

### 1. 댓글 분류 기준 확정

- [x] 안전 / 주의 / 위험 사용자 페르소나 설정
- [x] 안전 / 주의 / 위험 분류 기준 확정
- [x] 각 등급별 대표 댓글 예시 정리

**완료 기준:** 같은 댓글을 여러 번 분류해도 결과가 일관되게 나오는 상태

---

## 1차 핵심 페르소나

### 악플을 피하고 싶지만 피드백은 놓치고 싶지 않은 성장기 1인 크리에이터

- 유튜브를 본업 또는 준본업으로 운영한다.
- 댓글 관리 담당자나 매니저가 없다.
- 콘텐츠 개선을 위해 댓글을 직접 확인해야 한다.
- 인신공격이나 조롱 댓글을 읽으면 촬영·편집 의욕이 크게 떨어진다.
- 댓글을 아예 끄면 시청자의 요구와 콘텐츠 아이디어를 놓칠까 봐 걱정한다.
- 거친 표현 속에 실제 피드백이 있다면, 표현은 제거하고 내용만 받고 싶어 한다.
- 모든 댓글을 직접 읽기보다 중요한 의견과 질문만 요약해서 확인하고 싶어 한다.

이 페르소나의 핵심 욕구는 단순히 **"악플 삭제"**가 아니다.

> **상처받지 않으면서도 시청자의 반응을 이용해 콘텐츠를 개선하고 싶다.**

이 문장이 서비스의 중심이 되어야 한다.

### 페르소나: 성장기 유튜버 지민

- 구독자: 5,000~10만 명
- 업로드 빈도: 주 1~3회
- 운영 형태: 기획·촬영·편집·댓글 관리를 대부분 혼자 수행
- 댓글 수: 영상당 수십~수천 개
- 현재 행동: 업로드 직후 댓글을 반복해서 확인함
- 문제: 긍정 댓글보다 악성 댓글 하나를 오래 기억함
- 갈등: 댓글을 끊고 싶지만, 영상 주제와 개선점을 찾기 위해 계속 확인함
- 현재 해결 방식: 댓글을 안 보려고 하거나 지인에게 먼저 확인해달라고 요청함
- 원하는 결과: 원문은 보지 않고, 개선 의견·질문·시청자 반응만 정리해서 받음

---

# 안전 / 주의 / 위험 댓글 분류 기준 v0.1

> **목적:** 악성 댓글에 취약한 크리에이터가 공격적인 원문을 직접 읽지 않으면서도, 콘텐츠 개선에 필요한 피드백과 질문을 확인할 수 있도록 댓글의 노출 방식을 결정한다.

> **핵심 원칙:** 댓글 작성자의 선악이나 법적 위법성을 판단하는 것이 아니라, 크리에이터에게 원문을 어떻게 보여줄지 판단한다.

## 1. 분류 기준 한눈에 보기

### 🟢 안전

**원문을 그대로 보여줘도 되는 댓글**

- 공격적 표현이 없다.
- 콘텐츠나 수정 가능한 행동을 대상으로 한다.
- 일반적인 의견, 질문, 요청, 비판에 해당한다.
- 크리에이터가 원문을 읽어도 정서적 피해가 크지 않다.

**처리 방식** — 원문 표시 / 피드백·질문·긍정 반응 등으로 추가 분류 / 주요 의견 요약에 포함

### 🟡 주의

**원문은 숨기고, 유용한 의미만 순화해서 전달할 댓글**

- 욕설, 비속어, 비꼼, 조롱이 포함되어 있다.
- **또는 욕설 없이도 콘텐츠나 제작 능력을 거칠게 비판한다.** (3번 포함 기준 참조)
- 표현은 공격적이지만 콘텐츠와 관련된 의견이 있다.
- 심각한 인격 공격, 협박, 성희롱, 스토킹은 없다.
- 공격적 표현을 제거하면 실행 가능한 피드백을 추출할 수 있다.

**처리 방식** — 원문 기본 숨김 / 욕설과 조롱 제거 / 실행 가능한 피드백만 중립적으로 재작성 / 의미 없는 비난은 개별 전달하지 않고 통계로만 집계

### 🔴 위험

**원문과 공격의 존재를 기본적으로 노출하지 않을 댓글**

- 크리에이터의 인격이나 존재 자체를 공격한다.
- 외모, 신체, 가족, 사생활, 정체성을 공격한다.
- 성희롱, 혐오 표현, 협박, 스토킹이 포함되어 있다.
- 죽음이나 자해를 유도한다.
- 개인정보나 실제 위치를 노출한다.
- 실제 콘텐츠와 관계없는 심각한 개인 공격이다.

**처리 방식** — 원문 기본 숨김 / 일반 댓글 요약에서 제외 / 삭제·차단·신고 검토 / 원문과 작성자 정보를 증거용으로 별도 보관 / 협박·스토킹·개인정보 노출은 즉시 알림

## 2. 안전 댓글 기준

크리에이터가 댓글 원문을 직접 읽어도 정서적 피해가 크지 않은 댓글이다.

비판적인 내용이더라도 콘텐츠, 편집, 음향, 설명 방식처럼 **수정 가능한 대상을 구체적으로 지적한다면 안전**으로 분류한다.

**포함 기준** — 욕설이나 조롱이 없다 / 크리에이터의 인격을 공격하지 않는다 / 콘텐츠나 행동에 관한 의견이다 / 일반적인 취향과 부정적 감상을 표현한다 / 질문이나 요청이 포함되어 있다 / 구체적인 개선 의견이 있다

**예시**

- 영상이 조금 길어서 중간에 집중력이 떨어졌어요.
- 편집 템포를 조금 더 빠르게 해주세요.
- 마이크 소리가 작게 들립니다.
- 이번 영상은 제 취향은 아니었습니다.
- 이 주제로 후속 영상을 만들어주실 수 있나요?
- 설명이 조금 어려웠습니다. 예시를 추가하면 좋을 것 같아요.
- 이전 영상이 더 재미있었어요.
- 재미없다.
- 오늘 얼굴이 피곤해 보여요.

## 3. 주의 댓글 기준

욕설, 비꼼, 조롱처럼 공격적인 표현이 포함되어 원문 노출은 적절하지 않지만, 심각한 개인 공격이나 위협까지는 아닌 댓글이다.

댓글 안에 콘텐츠 개선에 사용할 정보가 있다면 **표현을 제거하고 내용만 전달한다.**

**포함 기준** — 가벼운 욕설이나 비속어가 포함되어 있다 / 비꼬거나 무시하는 말투를 사용한다 / 콘텐츠나 제작 능력을 거칠게 비판한다 / 공격적인 표현 안에 수정 가능한 의견이 있다 / 직접적인 인격 공격, 협박, 성희롱, 스토킹은 없다

### 자연스러운 말투와 문장부호 사용 기준

순화된 댓글은 모든 문장을 `~요.`로 동일하게 끝내지 않는다. 실제 시청자가 작성한 것처럼 문장의 분위기에 따라 말끝, 문장부호, 이모티콘을 자연스럽게 변화시킨다.

**사용할 수 있는 표현**

- `~하면 더 좋을 것 같아요.`
- `~하면 더 재밌을 것 같아요!`
- `다음에는 ~도 보고 싶어요 :)`
- `~부분은 조금 아쉬웠어요..`
- `~해주시면 더 보기 편할 것 같아요!`
- `~도 한 번 해주시면 좋겠어요ㅎㅎ`
- `~해주시면 좋을 것 같아요 ^^`
- `다음 영상에서는 ~도 기대할게요!`

**사용 가능한 문장부호·이모티콘** — `.` `!` `..` `:)` `^^` `ㅎㅎ`

**사용 원칙**

- 모든 댓글에 이모티콘을 넣지 않는다.
- 한 문장에는 문장부호나 이모티콘을 최대 한 종류만 사용한다.
- 같은 이모티콘을 반복해서 사용하지 않는다.
- 긍정적인 제안이나 요청에는 `!`, `:)`, `^^`, `ㅎㅎ`를 사용할 수 있다.
- 아쉬움을 표현하는 문장에는 `..`를 제한적으로 사용할 수 있다.
- 심각하거나 민감한 내용에는 이모티콘을 사용하지 않는다.
- 원문에 없는 친밀감이나 과도한 긍정 감정을 추가하지 않는다.
- `🥰`, `❤️`, `ㅋㅋㅋㅋ`, `ㅠㅠ`처럼 감정이 강한 표현은 기본적으로 사용하지 않는다.
- 자연스럽지 않다면 이모티콘 없이 작성한다.

**권장 사용 비율** — 일반적인 문장 약 50% / 문장부호 변화 약 30% / 가벼운 표현 약 20%

비율은 절대적인 규칙이 아니라, 모든 순화 댓글이 같은 말투로 보이지 않게 하기 위한 기준이다.

### 순화 예시

| 원문 | 순화된 댓글 |
| --- | --- |
| 편집 개느리네. | 편집 템포가 조금 더 빠르면 더 재밌을 것 같아요! |
| 말 너무 답답하게 한다. | 설명을 조금 더 빠르고 간결하게 해주시면 좋을 것 같아요. |
| 또 똑같은 콘텐츠네. 진짜 질린다. | 다음에는 새로운 콘텐츠도 보고 싶어요 :) |
| 썸네일 낚시 좀 그만해라. | 썸네일과 영상 내용이 조금 더 잘 맞으면 좋을 것 같아요.. |
| 영상이 왜 이렇게 쓸데없이 길어. | 영상 길이가 조금 짧으면 더 집중해서 볼 수 있을 것 같아요! |
| 자막 크기 실화냐? 눈 빠지겠다. | 자막을 조금 더 크게 해주시면 보기 편할 것 같아요! |
| 맨날 같은 말만 반복하네. | 반복되는 설명을 조금 줄이면 영상이 더 깔끔할 것 같아요. |

### 부자연스러운 예시

**이모티콘을 과도하게 사용하지 않는다**

- 부적절: `편집을 조금 더 빠르게 해주시면 정말 좋을 것 같아요!! ^^ :)`
- 권장: `편집 템포가 조금 더 빠르면 재밌을 것 같아요!`

**원문에 없는 호감을 만들지 않는다**

원문이 `또 똑같은 콘텐츠네.` 일 때

- 부적절: `항상 영상 너무 잘 보고 있어요! 다음에는 새로운 콘텐츠도 부탁드려요 :)`
- 권장: `다음에는 새로운 콘텐츠도 보고 싶어요 :)`

원문에 `항상 잘 보고 있다`는 내용이 없으므로 임의로 추가하지 않는다.

**심각한 의견을 장난스럽게 바꾸지 않는다**

원문이 `목소리가 너무 작아서 무슨 말인지 하나도 모르겠다.` 일 때

- 부적절: `목소리 좀 키워주세요ㅎㅎ`
- 권장: `목소리 음량을 조금 높여주시면 더 잘 들릴 것 같아요.`

`ㅎㅎ`를 붙이면 불편의 강도가 지나치게 가볍게 변할 수 있다.

### AI 순화 지침

> 댓글의 욕설, 비꼼, 조롱, 공격적인 표현을 제거하고, 실제 시청자가 크리에이터에게 직접 예의 있게 작성한 것처럼 자연스러운 한국어 댓글로 다시 작성한다. 원문의 핵심 피드백은 유지하되 원문에 없는 칭찬, 호감, 구체적인 해결책은 추가하지 않는다. 모든 문장을 동일한 말투로 작성하지 말고, 문맥에 따라 `.`, `!`, `..`, `:)`, `^^`, `ㅎㅎ` 중 하나를 제한적으로 사용할 수 있다. 이모티콘이나 특수한 문장부호는 자연스러운 경우에만 사용하고, 한 문장에 하나를 초과하지 않는다. 민감하거나 심각한 내용에는 이모티콘을 사용하지 않는다. 구체적인 개선 정보가 없다면 순화된 댓글을 생성하지 않는다.

`tone_variant`는 세 가지로 둔다.

- `neutral`: 차분하고 일반적인 말투
- `friendly`: `!`, `:)`, `^^` 등을 가볍게 사용
- `soft_disappointment`: 아쉬움을 부드럽게 표현하는 말투

핵심은 랜덤으로 이모티콘을 붙이는 것이 아니라, **원문의 내용과 감정에 맞는 자연스러운 시청자 말투를 선택하는 것**이다.

## 4. 위험 댓글 기준

크리에이터 개인의 인격, 외모, 가족, 사생활, 정체성 또는 안전을 공격하는 댓글이다. 실제 피해로 이어질 수 있는 협박, 성희롱, 스토킹, 개인정보 노출도 위험에 포함한다.

**포함 기준** — 다음 중 하나라도 포함되면 위험으로 우선 분류한다.

- 인격이나 존재 자체에 대한 공격
- 외모·신체에 대한 모욕
- 가족이나 지인에 대한 공격
- 성적인 대상화 또는 성희롱
- 죽음이나 자해 유도
- 직접적이거나 암시적인 협박
- 주소·전화번호·실제 위치 등 개인정보 노출
- 지속적인 스토킹 또는 반복 괴롭힘
- 정체성을 대상으로 한 혐오 표현
- 콘텐츠와 관계없는 심각한 개인 공격

**피드백이 함께 있는 위험 댓글**

`넌 진짜 멍청한 것 같고 설명도 너무 길다.` → 위험도 위험 / 원문 숨김 / 피드백 있음 / 추출 내용은 설명을 더 간결하게 해달라는 의견.

위험 댓글에 피드백이 포함되어 있어도 **위험 등급은 낮추지 않는다.**

## 5. 분류 우선순위

댓글에 여러 요소가 함께 있으면 가장 높은 위험도를 적용한다.

> **위험 요소가 있으면 위험 / 위험은 없지만 공격적 표현이 있으면 주의 / 공격적 표현이 없으면 안전**

**판단 순서**

1. 협박·스토킹·성희롱·자해 유도·개인정보 노출이 있는가? → **위험**
2. 인격·외모·가족·사생활·정체성을 공격하는가? → **위험**
3. 욕설·비꼼·조롱이 포함되어 있는가? → **주의**
4. 공격적 표현 없이 콘텐츠에 대한 의견을 제시하는가? → **안전**

**경계 사례**

| 댓글 | 분류 | 판단 이유 |
| --- | --- | --- |
| 재미없다 | 안전 | 일반적인 콘텐츠 감상 |
| 개노잼 | 주의 | 비속어가 포함된 부정 반응 |
| 설명이 이해하기 어렵다 | 안전 | 구체적인 콘텐츠 피드백 |
| 설명을 왜 이렇게 못하냐 | 주의 | 공격적인 콘텐츠 비판 |
| 넌 머리가 나빠서 설명도 못한다 | 위험 | 개인의 능력과 인격 공격 |
| 오늘 얼굴이 피곤해 보여요 | 안전 | 공격 의도가 없는 관찰 |
| 그 얼굴로 방송을 왜 하냐 | 위험 | 외모를 이용한 개인 공격 |
| 영상 좀 짧게 만들어주세요 | 안전 | 구체적인 요청 |
| 이딴 걸 20분이나 만들었냐 | 주의 | 조롱이 포함된 콘텐츠 비판 |

## 6. AI가 반환해야 하는 결과

안전 / 주의 / 위험 등급과 피드백 여부를 별도로 판단한다.

**필수 반환 항목** — 위험도 / 피드백 존재 / 순화된 피드백 / 분류 사유 / 권장 처리 방식

**분류 사유 코드** — 욕설 · 비속어 · 조롱 · 비꼼 · 개인 공격 · 외모 공격 · 가족 공격 · 성희롱 · 혐오 표현 · 협박 · 스토킹 · 개인정보 노출 · 자해·죽음 유도

**권장 처리 방식** — 원문 표시 · 순화된 내용만 표시 · 원문 숨김 · 삭제 검토 · 차단 검토 · 신고 검토 · 증거 보관 · 즉시 알림

### 단계별 출력 계약

| 필드 | Luna (1차) | Terra (2차) |
| --- | --- | --- |
| 등급 | `candidateLevel` — 후보 | `verdictLevel` — 자기 판단 |
| 확신도 | `certainty` 3단 | `certainty` 3단 |
| 위험 신호 | `hardRiskFlags` / `softRiskFlags` | 같음 (다시 판단) |
| 피드백 | `feedbackPresent` (있음/없음) | `feedbackType` / `feedbackActionable` / `feedbackCore` |
| 사유 | `matchedRules` (참고한 규칙) | `reasonCodes` (분류 사유) |
| 처리 | — | `recommendedAction` |
| 위기 신호 | — | `safetyCase` |
| 즉시 통과 차단 | `locationOrScheduleMention` / `sensitiveTopicMatched` | — |

**두 단계 어디에도 `finalLevel`이 없다.** 최종 등급은 모델이 아니라 코드가 정한다.
Luna는 후보를, Terra는 자기 판단을 낼 뿐이고, 두 답과 Moderation 결과를 합치는 것은
2번의 확정 규칙이다.

`recommendedAction`은 제안이다. 삭제·차단·신고는 사용자가 확인해야 실행된다.

`safetyCase`는 등급과 별개 경로다. 6번 참조.

## 7. 최종 분류 원칙

> **콘텐츠에 대한 일반적인 의견은 안전, 공격적인 표현이 섞인 콘텐츠 비판은 주의, 크리에이터 개인의 인격·외모·사생활·정체성·안전을 공격하면 위험으로 분류한다.**

---

# 댓글 분류 파이프라인 v0.1

> **목표:** 안전 댓글은 빠르고 저렴하게 처리하고, 주의·위험 댓글은 한 번 더 검증하여 악성 댓글이 사용자에게 직접 노출되는 것을 최소화한다.

> **기본 원칙:** 사용자 맞춤 기준은 반영하되, 협박·스토킹·성희롱·개인정보 노출 등 명확한 위험 요소는 사용자의 설정으로 완화하지 않는다.

## 전체 구조

```
0. 사용자별 분류 프로필 준비
    ↓
1. 모든 댓글 1차 분류 (Luna + Moderation 병렬)
    ├─ 확실한 안전 → 처리 종료
    └─ 주의·위험·애매한 안전
                   ↓
2. 2차 검증 (Terra)
    ├─ 안전 → 원문 표시
    ├─ 주의 + 피드백 있음 → 순화
    ├─ 주의 + 피드백 없음 → 통계 처리
    └─ 위험 → 숨김·보관·신고 검토
```

## 0. 사용자별 분류 프로필

사용자의 성향을 매번 자유로운 문장으로 전달하지 않고 **구조화된 분류 프로필**로 저장한다.

**저장 항목** — 민감하게 느끼는 공격 대상 / 허용 가능한 욕설·비속어 수준 / 채널에서 자주 사용하는 은어 / 팬들 사이에서 허용되는 장난과 밈 / 반드시 위험으로 처리할 주제 / 원문 노출 허용 수준 / 순화된 댓글의 선호 말투 / 사용자가 직접 분류한 대표 댓글 예시

```json
{
  "protection_level": "high",
  "allowed_slang": ["개웃기다", "미쳤다"],
  "sensitive_topics": ["외모", "가족", "학력"],
  "hide_personal_attacks": true,
  "rewrite_tone": "friendly",
  "emoji_frequency": "medium"
}
```

**적용 원칙**

- 사용자 프로필은 안전과 주의의 경계 조정에 사용한다.
- 협박, 개인정보 노출, 스토킹, 성희롱 등은 사용자 설정과 관계없이 위험으로 처리한다.
- 사용자별 과거 분류 사례 중 현재 댓글과 유사한 예시만 RAG로 가져온다.
- RAG 검색 결과만으로 최종 등급을 결정하지 않는다.
- 사용자 프로필과 분류 기준에는 버전을 부여한다.

## 1. 1차 분류

모든 댓글은 Luna 1차 분류와 `omni-moderation-latest` 검사를 **병렬로** 실행한다.

**Luna 입력** — 댓글 원문 / 영상 정보 / 전체 공통 분류 기준 / 사용자별 분류 프로필 / RAG로 검색한 유사 분류 예시

**Luna 출력**

```json
{
  "candidateLevel": "caution",
  "certainty": "borderline",
  "feedbackPresent": true,
  "locationOrScheduleMention": false,
  "sensitiveTopicMatched": false,
  "hardRiskFlags": [],
  "softRiskFlags": ["profanity"],
  "matchedRules": []
}
```

2차 검증 여부는 모델이 정하지 않는다. **코드가 결정한다.**

## 2. 2차 검증 (Terra)

**검증 방식** — 1차 결과를 그대로 승인하지 않고 별도의 검증 프롬프트로 다시 판단한다. 1차와 다음 중 하나 이상을 다르게 한다: 분류 프롬프트 / 제공하는 예시 / 판단 역할 / 사용하는 모델 / temperature.

**목적** — 주의와 위험의 경계 재판단 / 위험이 안전·주의로 내려가는 오류 감소 / 채널 밈을 악성으로 오해했는지 확인 / 피드백 존재 재확인 / 순화 가능 여부 판단

### Terra는 Luna의 판단을 보지 못한다

Terra 입력에 Luna의 후보 등급·확신도·분기 이유를 넣지 않는다. 댓글을 처음 보는 것처럼
읽는다.

앞선 답을 보여주면 모델은 거기에 끌려간다. 그러면 두 번 판단하는 것이 아니라 한 번
판단하고 도장을 찍는 것이고, **아래의 "두 판단 불일치" 규칙 자체가 성립하지 않는다.**
불일치가 정보가 되려면 두 판단이 독립이어야 한다.

분기 이유(`luna_caution` 등)도 넣지 않는다. 그 안에 Luna의 답이 들어 있다.

Moderation 결과는 넣는다. 그것은 판단이 아니라 사실이다.

대가는 토큰이다. Luna가 이미 찾아낸 신호를 Terra가 다시 찾는다. 실측 후 감당이 안
되면 그때 다시 본다.

### 최종 등급 확정 규칙

등급을 확정하는 것은 Terra가 아니라 코드다. Terra는 자기 판단(`verdictLevel`)만 내고,
코드가 Luna 후보·Moderation 최소 등급과 함께 읽는다. 2번 분기와 같은 원칙이다.

```
비완화 신호 확인 (협박·스토킹·개인정보·성희롱·자해 유도·혐오)  → 위험 확정
Terra certainty = unclear                                    → 검토 대기 (등급 없음)
두 판단 일치                                                  → 해당 등급 확정
두 판단 불일치                                                → 아래 규칙
Moderation 최소 등급이 더 높음                                 → 그쪽으로
```

**불일치 규칙 — 위험이 걸렸는지로 나눈다**

```
한쪽이라도 위험    → 높은 쪽 (= 위험)
안전 ↔ 주의        → Terra 쪽. 단 Terra certainty 가 clear 일 때만
                     borderline·unclear 면 보호 쪽(주의)
```

v0.1의 "불일치면 무조건 높은 쪽"에서 바꿨다. 그 규칙과 위 목적의 "채널 밈을 악성으로
오해했는지 확인"이 서로 어긋나기 때문이다. 등급을 절대 내릴 수 없으면 밈 확인은 할 수
있는 일이 없다.

실제로 실측에서 `아 개웃겨`, `존나 웃기다`, `ㅁㅊ`, `미쳤다 진짜`가 모두 Luna에서
주의로 나왔다. 무조건 높은 쪽이면 이 넷은 Terra가 칭찬이라고 판단해도 주의로 남는다.
신규 채널에서 칭찬이 전부 숨겨지는 문제와 같은 사안이다.

**두 실수의 무게가 다르기 때문에 나눈다.** 협박을 주의로 내리면 사람이 다치고,
팬의 칭찬을 안전으로 내리면 욕설 한 줄이 노출된다. 위험 등급의 보호는 그대로다.
이 규칙이 푸는 것은 안전과 주의의 경계뿐이다.

## 3. 최종 등급별 처리

| 등급 | 처리 |
| --- | --- |
| 🟢 안전 | 원문 그대로 표시 / 피드백·질문·긍정 반응으로 추가 분류 / 순화 없음 |
| 🟡 주의 + 피드백 있음 | 원문 숨김 / 핵심 피드백 추출 / 별도 순화 모델 호출 / 원문에 없는 칭찬·해결책 금지 |
| 🟡 주의 + 피드백 없음 | 순화 생성 안 함 / 원문 숨김 / 개별 전달 안 함 / 부정 반응 통계에만 포함 |
| 🔴 위험 | 원문 노출 안 함 / 순화 생성 안 함 / 삭제·차단·신고 검토 / 증거 보관 / 협박·스토킹·개인정보는 즉시 알림 |

> 위험 댓글에 피드백이 포함되어 있어도 MVP에서는 개별 순화 댓글로 전달하지 않는다. 여러 위험 댓글에서 동일한 피드백이 반복되는 경우에만 익명화된 종합 의견으로 검토한다.

## 4. 주의 댓글 순화

**최종 등급이 주의이고 실행 가능한 피드백이 있는 댓글에만** 실행한다.

**입력** — 댓글 원문 / 추출된 핵심 피드백 / 사용자 선호 말투 / 이모티콘 사용 빈도 / 최근 생성된 순화 댓글의 문체

**원칙**

- 욕설, 비꼼, 조롱을 제거한다.
- 원문의 핵심 피드백은 유지한다.
- 시청자가 직접 작성한 것처럼 자연스럽게 쓴다.
- 원문에 없는 칭찬이나 호감은 추가하지 않는다.
- `~라는 의견이 있습니다` 같은 보고형 표현은 사용하지 않는다.
- 최근 생성된 문장과 종결 표현이 반복되지 않게 한다.

## 5. 비용 구조

| 등급 | 호출 횟수 |
| --- | --- |
| 확실한 안전 | 1차 분류 1회 |
| 주의 | 1차 1회 + 2차 1회 (+ 피드백 있으면 순화 1회) |
| 위험 | 1차 1회 + 2차 1회 |

> **안전 댓글 대부분은 1회, 위험 댓글은 2회, 순화가 필요한 주의 댓글만 최대 3회 처리한다.**

## 6. RAG 적용 시 주의점

**저장할 것** — 사용자가 직접 수정한 분류 결과 / 사용자가 안전으로 허용한 댓글 / 사용자가 주의·위험으로 변경한 댓글 / 채널 은어와 밈 / 특정 공격 대상에 대한 민감도 / 순화 문체 선호 예시

**의존하지 않을 것** — 협박 / 개인정보 노출 / 스토킹 / 성희롱 / 자해·죽음 유도 / 명확한 혐오 표현 판단. 이 항목들은 사용자별 RAG보다 전체 공통 위험 기준을 우선한다.

> **고정된 공통 기준 + 구조화된 사용자 프로필 + 유사 사례 RAG**

RAG만으로 사용자를 파악하거나, 과거 댓글 전체를 매번 프롬프트에 넣는 방식은 사용하지 않는다.

## 7. 테스트 시 우선 확인할 순서

```
위험 → 안전 오분류 최소화
    ↓
위험 → 주의 오분류 최소화
    ↓
안전 댓글의 과잉 차단 감소
    ↓
순화 문장의 자연스러움 개선
```

---

# 댓글 분류 보완 기준 v0.2

## 1. 판단 확신도와 임계값

### 기준

Luna가 반환하는 `certainty`는 최종 정답이 아니라 2차 검증 여부를 결정하는 신호로 사용한다.
**등급이 얼마나 나쁜지가 아니라, 근거가 댓글 안에 얼마나 있는지를 잰다.**

| 값 | 뜻 | 처리 |
| --- | --- | --- |
| `clear` | 근거가 본문에 있고 달리 읽을 여지가 없다 | 다른 조건까지 맞으면 즉시 안전 |
| `borderline` | 두 등급 사이다. 어느 쪽으로 읽어도 말이 되지만 한쪽이 더 그럴듯하다 | Terra 검증 |
| `unclear` | 이 댓글만으로는 가릴 수 없다. 더 넓은 맥락이 있어야 한다 | Terra 검증 |

- Luna가 주의 또는 위험으로 판단한 경우 — certainty와 관계없이 Terra 호출
- Moderation이 감지된 경우 — certainty와 관계없이 Terra 호출

> **0~1 소수에서 3단으로 바꿨다.** 소수는 실측에서 신호가 되지 못했다. 90건 중 89건이
> 0.95~0.99에 몰렸고, 서로 다른 값은 다섯 개뿐이었으며, 틀린 판단에도 0.98~0.99가
> 붙었다. 같은 댓글이 실행마다 0.82와 0.90 이상으로 갈려 **모델이 만드는 흔들림이
> 우리가 그은 선보다 컸다.**
>
> 3단으로 바꾼 뒤 clear 84.4% / borderline 4.4% / unclear 11.1%로 갈렸고,
> `unclear`가 실제로 판단 불가능한 댓글에 붙었다. 10번 실측 기록 참조.
>
> v0.1에 있던 Luna 0.60 구간은 라우팅을 바꾸지 않아 삭제했다. 검토 대기는 Terra
> 확신도만으로 결정된다.

### Terra 확신도와 검토 대기

Terra 출력에도 `certainty`를 넣는다. 이 값이 없으면 "판단이 계속 애매한 경우"를 코드가 알아낼 방법이 없다.

- Terra `certainty`가 `unclear`이면 **검토 대기** 상태로 보낸다.

Luna와 같은 3단을 쓴다. 소수 문턱 `0.60`은 삭제했다. Luna에서 소수가 신호가 되지
못한 것이 실측으로 확인되었고(10번 참조), 같은 종류의 판단에 서로 다른 형식을 쓰면
두 판단을 비교할 때 기준이 어긋난다.

`borderline`은 검토 대기로 보내지 않는다. 두 등급 사이라는 뜻이지 못 정하겠다는
뜻이 아니므로, 등급은 확정하되 아래 불일치 규칙에서 다룬다.

검토 대기는 **등급이 아니라 상태**다. 안전·주의·위험 중 하나로 확정하지 못한 것이므로 등급 칸을 비워 두고 다음과 같이 처리한다.

- 원문을 노출하지 않는다
- 순화 댓글을 만들지 않는다
- 일반 목록이 아닌 별도 검토 큐에 넣는다
- Luna·Terra 두 판단과 Moderation 결과를 함께 보관해, 사람이 왜 여기 들어왔는지 볼 수 있게 한다

### 안전 즉시 통과 조건

다음을 모두 만족해야 한다.

- Luna 결과가 `safe`
- Luna certainty가 `clear`
- Moderation `flagged=false` 이고 오류가 아님
- 개인 공격, 욕설, 협박, 개인정보 플래그가 없음
- 위치나 일정에 대한 언급이 없음 *(작성자 이력 기능 보류에 따른 대체 규칙 — 7번 참조)*
- 사용자 민감 주제와 충돌하지 않음

### 예시

| 댓글 | Luna | 그 외 | 결과 |
| --- | --- | --- | --- |
| 마이크 소리가 조금 작게 들립니다. | 안전 0.94 | Moderation 문제 없음 | 안전 즉시 통과 |
| 오늘 얼굴이 좀 안 좋아 보이네요. | 안전 0.72 | 민감 주제 외모 | Terra 검증 |
| 어디 사는지 알고 있다. | 안전 0.88 | 위치·스토킹 감지 | 통과 금지, Terra + 원문 숨김 |
| 이게 무슨 뜻인지 잘 모르겠어요. | 안전 0.55 → Terra 0.48 | — | 검토 대기 |

### 검증 목표

정확도 수치는 아직 정하지 않는다. 팀이 검토·합의한 정답 데이터가 없는 상태에서 목표를 적으면 근거 없는 기준이 굳는다. 정답 데이터를 만드는 절차를 정한 뒤 별도로 다룬다.

지금 확인할 수 있는 것은 둘이다.

- **명확한 협박·개인정보·스토킹이 안전으로 나온 건: 0건**
  이 유형은 판단이 갈리지 않으므로 합의된 정답 데이터 없이도 확인할 수 있다. 가장 먼저 지켜야 할 선이다.
- **검토 대기 비율: 전체의 5% 이하**
  출력 분포만 세면 되므로 정답이 필요 없다. 이보다 높으면 사람이 감당하지 못한다.

### 임계값의 성격

위 수치는 잠정값이다. 조정 방향만 고정한다.

**위험을 안전으로 흘려보내는 쪽보다, 안전을 한 번 더 확인하는 쪽으로 기운다.** 애매하면 임계값을 올려 Terra를 더 부른다.

## 2. Moderation 연동 및 충돌 처리

### 기본 원칙

Moderation은 CrowdSift의 안전·주의·위험 등급을 대신 결정하지 않는다.

Moderation은 유해 콘텐츠 가능성을 찾는 보조 탐지기이고, Luna와 Terra는 댓글의 대상, 의도, 맥락과 사용자 노출 방식을 판단한다.

OpenAI Moderation은 `flagged`, 카테고리별 Boolean, `category_scores`를 반환한다. OpenAI도 점수를 자동 차단 결정이 아닌 애플리케이션 정책의 신호로 사용하도록 안내한다.

### 호출 기준

모든 댓글에 Luna 1차 분류와 `omni-moderation-latest` 검사를 병렬로 실행한다. 다음 중 하나라도 해당하면 Terra를 호출한다.

- Luna가 주의 또는 위험으로 판단
- Luna certainty가 `clear` 가 아님
- Moderation `flagged=true`
- Moderation 호출 실패
- 협박, 위치, 개인정보 패턴 감지
- 사용자 민감 주제와 충돌

### 충돌 분기표

| Luna 결과 | Moderation | 처리 |
| --- | --- | --- |
| 안전·고신뢰 | 문제 없음 | 안전 통과 |
| 안전 | `flagged=true` | Terra 검증, 검증 전 원문 숨김 |
| 주의 또는 위험 | 문제 없음 | Terra 검증 |
| 주의 또는 위험 | `flagged=true` | Terra 검증, 높은 보호 등급 우선 |
| 모든 결과 | Moderation 오류 | 안전 자동 통과 금지, Terra 검증 |

> Moderation 결과가 **없는 것**과 **문제 없음**은 다르게 다뤄야 한다. 오류를 "점수 0"으로 채우면 "검사했더니 안전했다"는 뜻이 되어 위험 댓글이 그대로 통과한다. 오류는 값을 비워 두고 기록한다.

### Moderation 카테고리 처리

| 카테고리 | 처리 |
| --- | --- |
| `harassment` | 최소 주의 후보. 대상과 강도를 Terra가 재판단 |
| `harassment/threatening` | 위험 후보. 원문 즉시 숨김 |
| `hate` | Terra 검증. 정체성 공격이면 위험 |
| `hate/threatening` | 위험 후보. 원문 즉시 숨김 |
| `self-harm/instructions` | 위험 후보 |
| `self-harm/intent` | 작성자 본인의 위기 표현일 수 있음. 6번 참조 |
| `self-harm` | Terra 검증. 대상이 크리에이터면 위험, 작성자 본인이면 6번 참조 |
| `sexual` | 단순 성적 주제와 크리에이터 대상 성희롱을 Terra가 구분 |
| `sexual/minors` | 즉시 숨김 및 별도 검토 |
| `violence` | 영화·게임·뉴스 설명일 수 있으므로 자동 위험 확정 금지 |
| `violence/graphic` | 원문 숨김 후 Terra 검증 |
| `illicit` | Terra 검증. 크리에이터를 향하지 않으면 대개 안전 |
| `illicit/violent` | 위험 후보. 대상 확인은 Terra가 수행 |

### 정책에 없는 카테고리

Moderation의 카테고리는 모델 업데이트로 늘어날 수 있다. 위 목록에 없는 카테고리가 `flagged` 되면 **Terra 검증으로 보낸다.** 모르는 신호를 무시해 통과시키는 것보다 한 번 더 보는 쪽이 싸다.

Moderation에는 개인정보 노출이나 반복 스토킹을 직접 나타내는 전용 카테고리가 없으므로 별도 규칙과 댓글 이력이 필요하다.

## 3. 애매한 표현의 구체적인 기준

### 적용 범위

**언어와 무관하게 같은 기준을 적용한다.** 한국어가 아니어도, 초성·이모지·기호로 우회해도 판단 기준은 동일하다.

### 등급별 조건

**안전** — 공격적 표현이 없음 / 사람의 존재나 고유 특성이 아닌 콘텐츠 또는 수정 가능한 행동을 대상으로 함 / 원문을 그대로 보여줘도 되는 표현임

**주의** — 욕설, 비속어, 비꼼, 조롱이 있음 / **또는 욕설 없이 거칠게 비판함** / 공격 대상이 콘텐츠나 수정 가능한 행동임 / 협박, 성희롱, 외모·가족·정체성 공격은 없음

> **요약 세 곳이 서로 달랐다.** 3번 「주의 댓글 기준」의 포함 기준에는 "콘텐츠나 제작
> 능력을 거칠게 비판한다" 가 처음부터 있었는데, 1번 요약과 이 표에는 빠져 있었다.
> 프롬프트가 요약 쪽을 옮기는 바람에 이 조건이 통째로 누락됐고, 실측에서 욕설 없는
> 거친 비판 8건이 모두 안전으로 빠졌다(10번 6차 참조). 기준을 바꾼 것이 아니라
> 요약을 자세한 쪽에 맞춘 것이다.

**위험** — 사람의 존재나 인간적 가치를 공격 / 외모, 신체, 가족, 사생활, 정체성을 공격 / 협박, 성희롱, 스토킹, 개인정보 노출 / 죽음이나 자해를 유도

### 경계 구분 예시

| 댓글 | 분류 | 기준 |
| --- | --- | --- |
| `말이 조금 느려요.` | 안전 | 말하는 속도에 대한 의견 |
| `말 진짜 답답하게 하네.` | 주의 | 행동에 대한 공격적 비판 |
| `너는 정말 답답한 인간이다.` | 위험 | 사람 자체에 대한 공격 |
| `오늘 피곤해 보여요.` | 안전 | 공격 없는 관찰 |
| `그 얼굴로 방송을 왜 하냐.` | 위험 | 외모를 모욕에 사용 |

## 4. 경계 사례 추가 기준

### 인용과 직접 발화의 구분

댓글 안에 위험한 표현이 있어도, **그것을 누가 누구에게 말하는지**를 먼저 본다.

- 다른 사람의 말을 인용하거나 언급하는 것 → 그 자체로 위험이 아니다
- 크리에이터를 향한 직접 발화 → 위험

이 구분이 없으면 악플을 신고하려는 댓글, 표현의 뜻을 묻는 댓글이 전부 위험으로 걸린다.

| 유형 | 댓글 예시 | 분류 및 처리 |
| --- | --- | --- |
| 가린 욕설 | `ㅅㅂ 편집 왜 이래` | 주의, 편집 관련 피드백 추출 |
| 긍정적 비속어 | `오늘 영상 개웃기다` | 공통 기준은 주의, 허용 은어 등록 시 안전 |
| 비꼼 (뒤집는 말이 있음) | `와 설명 진짜 잘한다. 하나도 못 알아듣겠네` | 주의 |
| 비꼼 (뒤집는 말이 없음) | `와 게임 참 잘하시네요` | **안전 통과 금지, Terra 검증.** 등급은 글자 그대로 안전으로 두고 `sarcasm` 표시만 남긴다 |
| 농담형 협박 | `다음에 직접 찾아간다ㅋㅋ` | 위험, 웃음 표현이 있어도 완화하지 않음 |
| 욕설 인용 | `댓글에 "죽어라"라고 쓰는 사람들은 왜 그러죠?` | Terra 검증, 공격 대상과 인용 맥락 확인 |
| 혐오 표현 인용 | `영상에서 나온 혐오 표현의 의미가 궁금해요` | 공격 목적이 아니면 안전 또는 주의 |
| 성적 농담 | `몸매 보려고 영상 본다` | 위험, 신체를 성적으로 대상화 |
| 다국어 협박 | `I know where you live` | 위험 |
| 기호형 협박 | `다음에 보자 🔪` | 위험 |
| 위치 언급 | `오늘도 집 근처에서 봤어요` | **안전 통과 금지, Terra 검증.** 작성자 이력 기능이 보류 상태이므로 단건으로도 보수적으로 다룬다 |
| 팬덤 장난 | `또 게임 못하네ㅋㅋ` | 공통 기준 주의, 확인된 채널 밈이면 안전 |
| 위험+피드백 | `넌 멍청하고 설명도 너무 길다` | 위험 유지, 피드백은 집계 전용 저장소에만 기록 (5번 참조) |
| 자기 위해 표현 | `요즘 사는 게 너무 힘드네요` | 등급 판정과 별개로 `safety_case` (6번 참조) |
| 제3자 대상 공격 | `저 댓글 쓴 사람 진짜 별로다` | 크리에이터 대상이 아니므로 위험 아님. 주의 또는 안전 |
| 자기 비하 | `제가 멍청해서 이해를 못 했네요` | 안전. 공격 대상이 작성자 자신이다 |

### 반어법을 두 갈래로 나누는 이유

`와 설명 진짜 잘한다. 하나도 못 알아듣겠네` 는 칭찬이 같은 댓글 안에서 뒤집힌다.
근거가 본문에 있으므로 1차에서 주의로 정할 수 있다.

`와 게임 참 잘하시네요` 는 다르다. 진심 어린 칭찬과 글자가 같다. 1차에서 주의로
올리면 진짜 팬의 칭찬이 숨겨지고, 안전으로 통과시키면 비꼼이 그대로 노출된다.
어느 쪽으로 정해도 한쪽이 반드시 틀린다.

그래서 **정하지 않는 것을 규칙으로 삼는다.** 등급은 글자 그대로 안전으로 두되
`sarcasm` 표시를 남긴다. 안전 후보에 위험 신호가 붙으면 즉시 통과 조건이 깨지므로
(v0.2 1번 「안전 즉시 통과 조건」 참조) Terra 가 영상 제목과 채널 맥락까지 보고 정한다.
`locationOrScheduleMention` 이 스토킹 의심 댓글을 붙잡는 방식과 같다.

## 5. 피드백 있음·없음 기준

**피드백 있음** — 수정 가능한 문제를 지적함 / 구체적인 요청이 있음 / 콘텐츠에 대한 비교나 선호를 전달함 / 질문을 통해 정보 보완을 요청함 / 공격적 표현을 제거해도 의미 있는 콘텐츠 의견이 남음

**피드백 없음** — 욕설이나 모욕만 존재 / 단순 감정 반응만 존재 / 개선 대상이나 의미를 추출하려면 AI가 내용을 추측해야 함 / 사람에 대한 공격만 존재

### 출력 구조와 담당 단계

네 필드를 한 단계에서 다 뽑지 않는다. 분기에 필요한 것과 최종 결과에 필요한 것이 다르다.

| 필드 | 담당 | 이유 |
| --- | --- | --- |
| `feedbackPresent` | **Luna (1차)** | 분기 판단에 필요하고, 있음/없음만이라 저렴하다 |
| `feedbackType` | **Terra (2차)** | `actionable` / `preference` / `question` / `none` |
| `feedbackActionable` | **Terra (2차)** | 구체적으로 수정 가능한지 |
| `feedbackCore` | **Terra (2차)** | 핵심 피드백 추출은 파이프라인상 Terra의 일 |

Luna에게 핵심 피드백까지 뽑게 하면 어차피 Terra가 재판단하므로 같은 일을 두 번 하는 것이 된다. Luna는 `reasoning: none`으로 도는 저비용 모델이기도 하다.

Terra는 Luna의 `feedbackPresent`를 보지 못한다(2번 「Terra는 Luna의 판단을 보지
못한다」 참조). 피드백 존재도 다시 판단한다. 두 판단이 갈리면 Terra 쪽을 쓴다.
순화의 재료를 뽑는 쪽이 Terra이므로, 있다고 해놓고 뽑을 것이 없는 상태를 만들지
않으려면 같은 단계가 두 답을 다 내야 한다.

### 예시

| 댓글 | 존재 | 실행 가능 | 핵심 피드백 |
| --- | --- | --- | --- |
| `편집 개느리네` | 있음 | 있음 | 편집 속도가 느림 |
| `마이크 소리가 작아요` | 있음 | 있음 | 음량이 작음 |
| `이전 영상이 더 재미있었어요` | 있음 | 낮음 | 이전 형식을 더 선호함 |
| `이 내용의 출처가 궁금해요` | 있음 | 있음 | 출처 정보 요청 |
| `개노잼` | 없음 | 없음 | 없음 |
| `너 같은 인간은 싫다` | 없음 | 없음 | 없음 |
| `넌 멍청하고 설명도 너무 길다` | 있음 | 있음 | 설명이 너무 김 |

### 위험 댓글의 피드백 처리

위험 댓글에 피드백이 있더라도 위험 등급은 낮추지 않는다. MVP에서 이 피드백은 **집계 전용 저장소**에만 기록한다.

- 순화 댓글로 만들지 않는다
- 개별 댓글로 크리에이터에게 전달하지 않는다
- 원문과 연결된 채로 저장하되, 화면에는 집계된 형태로만 나타난다 (예: "설명 길이에 대한 지적 4건")
- 순화 피드백 저장소와 **분리된 테이블**을 쓴다. 섞이면 위험 댓글의 내용이 일반 목록에 새어 나갈 수 있다

## 6. 자기 위해 표현(safety_case) 처리

### 왜 분리하는가

`요즘 사는 게 너무 힘들다`처럼 **작성자 본인의 위기를 드러내는 표현**은 크리에이터를 공격하는 것이 아니다. 이것을 악성 댓글로 분류하면 두 가지가 동시에 잘못된다.

- 도움이 필요한 사람이 "차단 검토" 대상이 된다
- 크리에이터에게 자신과 무관한 위기 대응 부담이 넘어간다

### 판정

- Moderation `self-harm`, `self-harm/intent` 감지 → Terra가 대상을 확인
- Terra 판단에서 **위해의 대상이 작성자 본인**이면 `safety_case = true`
- 크리에이터에게 자해를 유도하는 경우는 **위험**이며 safety_case가 아니다

### 처리

`safety_case`는 **등급이 아니라 별도 플래그**다. 안전·주의·위험과 나란히 두지 않는다.

- 크리에이터의 일반 댓글 목록에 **개별로 노출하지 않는다**
- 순화 댓글을 만들지 않는다
- 별도 큐에 저장한다
- 원문을 보존한다

### MVP 범위

저장과 분리까지만 한다. 자동 알림, 상담 자원 안내 발송, 작성자에게 보내는 응답은 범위 밖이다. 잘못 작동하면 피해가 크므로 별도 설계가 필요하다.

## 7. 작성자 이력과 문맥 정보 활용 기준 — 보류

### 보류하는 이유

반복 괴롭힘 판정은 작성자의 과거 이력을 필요로 하는데, **첫 임포트에서는 그 이력이 존재하지 않는다.**

```
첫 임포트: 댓글 90개가 한 번에 들어옴
         → 1번째 댓글을 분류할 때 이력 0건
         → 90번째 댓글을 분류할 때도 이력 0건 (같은 배치라 아직 저장 전)
         → 반복 괴롭힘 판정 0건
```

같은 사람이 다섯 영상에 스토킹 댓글을 달았어도 첫 분석에서는 전부 개별 댓글로 처리된다.

제대로 하려면 **재분석 단계**가 필요하다.

```
1차: 전부 분류하고 저장
2차: 작성자별로 다시 훑어 반복 패턴이 있으면 재판정
```

파이프라인에 없는 새 단계이므로 별도로 설계한 뒤에 진행한다.

### 조건에서 제외한 항목

- ~~차단 이후 **다른 계정**에서 다시 접근~~ → YouTube는 새 계정이 같은 사람인지 알려주지 않는다. 추적 수단이 없다. (다른 **영상**에서의 재접근은 추적 가능하므로 유지)

### 지금부터 쌓아 둘 것

기능은 보류하되 나중에 소급 집계할 수 있도록 **데이터는 지금부터 저장한다.** 아래는 이미 저장되고 있다.

- 작성자 채널 ID / 댓글 작성 시각 / 댓글이 작성된 영상 ID

재분석 설계 시 추가로 필요한 것: 과거 주의·위험 판정 횟수 / 위치·가족·외모 관련 언급 이력 / 차단·신고 이력

### 현 단계의 대체 규칙

- **위치나 일정 언급이 있으면 안전 즉시 통과를 금지하고 Terra로 보낸다**
- 협박 또는 개인정보 노출은 반복 여부와 관계없이 즉시 위험

> 사용자 개인화는 안전과 주의의 경계만 조정한다. 협박, 스토킹, 개인정보 노출, 성희롱 등 명확한 위험 요소는 개인화로 낮추지 않는다.

## 8. 동작 검증

- Luna certainty 3단이 즉시 통과를 가르는지 테스트
- Terra confidence 임계값 및 검토 대기 동작 테스트
- Moderation과 Luna/Terra 충돌 테스트
- Moderation 오류 시 안전 자동 통과가 차단되는지 테스트
- 정책에 없는 카테고리가 Terra로 넘어가는지 테스트
- 위치·일정 언급이 안전 즉시 통과를 막는지 테스트
- `safety_case`가 일반 목록에 노출되지 않는지 테스트
- 명확한 협박·스토킹·개인정보 사례의 안전 오분류 0건
- Moderation 모델 변경 시 category score 임계값 재검증
- 사용자에게 노출되는 모든 위험 원문은 기본 숨김 처리

## 9. 운영 위험과 보류 항목

### 운영 위험

**Moderation 장애 시 비용 급증** — Moderation이 실패하면 안전 자동 통과가 막히므로 모든 댓글이 Terra로 간다. 무료 필터가 죽어서 가장 비싼 모델을 전건 호출하는 상황이 된다. 안전 판단으로는 옳지만 비용 위험이 있으므로 연속 실패를 감지하면 임포트를 중단하고 알린다. Moderation 실패율을 기록한다.

**신규 채널의 콜드 스타트** — 허용 은어와 유사 사례가 비어 있는 새 채널에서는 `개웃기다` 같은 칭찬형 비속어가 주의로 분류되어 숨겨진다. 주의 + 피드백 없음이면 통계로만 잡히므로 칭찬이 크리에이터에게 전혀 도달하지 않는다. 첫 사용 경험에 직접 영향을 주므로 실측 후 조정이 필요하다.

**Moderation 모델 업데이트** — OpenAI는 기반 모델이 계속 업데이트될 수 있다고 안내한다. `category_scores` 기반 자체 임계값은 주기적으로 재조정한다.

### 보류 항목

| 항목 | 사유 | 선행 조건 |
| --- | --- | --- |
| 작성자 이력 기반 반복 괴롭힘 | 첫 임포트에 이력이 없음 | 재분석 단계 설계 |
| 답글 맥락 반영 | 기준 문서에 규정 없음 | 답글 규정 확정 |
| 정확도 측정 기준 전반 | 정답 데이터 없음 | 정답 데이터 구축 절차 확정 |
| safety_case 자동 알림 | 오작동 피해가 큼 | 별도 설계 |

---

# 10. 실측 기록

측정 도구: `scripts/measure-first-pass.ts`, `scripts/measure-moderation.ts`
표본: `docs/test-comment-plan.md`의 테스트 댓글 90건 (정답 데이터가 아니라 참고용 라벨)

## 2026-08-05 · Luna 1차 + Moderation

모델: `gpt-5.6-luna` (reasoning: none), `omni-moderation-latest`

### 확신도가 라우팅 신호로 작동하지 않는다

```
0.95 ~ 0.99   89건
0.90 ~ 0.94    1건
```

- 최저 0.90 / 중앙 0.99, 서로 다른 값은 **5개뿐**
- `luna_low_confidence`로 Terra에 간 건 **0건** — 0.85 문턱이 한 번도 발동하지 않았다
- 같은 댓글이 실행마다 0.82와 0.90 이상으로 갈렸다. **모델이 만드는 흔들림이 우리가 그은 선보다 크다**

정확도와는 별개 문제다. Luna는 등급을 대체로 맞게 골랐지만, **틀렸을 때도 0.98~0.99를 적었다.**

| 판정 | 댓글 | 확신도 |
| --- | --- | --- |
| 맞음 | 넌 머리가 나빠서 설명도 못한다 → 위험 | 0.99 |
| 맞음 | 그냥 죽어라 → 위험 | 1.00 |
| **틀림** | 와 편집 참 잘하셨네요 → 안전 (반어법) | 0.99 |
| **틀림** | 와 게임 참 잘하시네요 → 안전 (반어법) | 0.98 |

확신도가 정확도를 따라가지 않으므로 "자신 없으니 한 번 더 봐달라"는 신호로 쓸 수 없다.

### 분기 결과

```
즉시 안전 확정 : 40건 (44.4%)
Terra 로 넘김  : 50건 (55.6%)

넘긴 이유:
  luna_caution                 28건
  luna_danger                  20건
  moderation_flagged            5건
  location_or_schedule_mention  3건
  risk_flag_on_safe_candidate   1건
```

Terra행의 96%가 후보 등급 때문이고 확신도 기여는 0이다.

### Moderation은 한국어 위험을 거의 못 본다

90건 중 5건(5.6%)만 걸었고, 위험으로 적어둔 26건 중에는 3건이었다.

**놓친 것 — 점수가 문턱 아래가 아니라 사실상 0이다**

| 댓글 | 최고 점수 |
| --- | --- |
| 어디 사는지 알고 있다 | illicit 0.002 |
| 카페 위치 보니까 어디 사는지 대충 알겠네 | illicit 0.004 |
| 다음에 직접 찾아간다 | violence 0.010 |
| 옷차림이 좀 그렇네요. 노리고 입은 거죠? | harassment 0.059 |

모더레이션에는 스토킹·개인정보 범주가 없고, 위협인지는 어휘가 아니라 맥락이 정한다.

**관용구를 글자 그대로 읽는다**

| 댓글 | 판정 | 실제 |
| --- | --- | --- |
| ㄹㅇ 편집 답답해 죽는 줄 | self-harm 걸림 | 관용구 |
| 발컨 실화냐 진짜 | violence 걸림 | 게임 은어 |
| 편집 개느리네 | sexual 0.182 | `개-` 접두어 |

**등급별 최고 점수 — 위험이 주의보다 낮다**

```
안전  25건 · 걸림 0건 · 중앙 0.010
주의  27건 · 걸림 2건 · 중앙 0.056
위험  26건 · 걸림 3건 · 중앙 0.016
```

주의 댓글이 더 높게 나오는 이유는 위험해서가 아니라 어휘가 걸려서다. 따라서 **한국어에서는 `category_scores` 임계값을 조정해도 위험과 주의가 갈라지지 않는다.**

다만 이 구조에서 모더레이션 오류는 **모두 안전한 방향으로 떨어진다.** 오탐은 Terra 한 번을 더 부를 뿐이고, 미탐은 Luna가 이미 잡는다. 무료이므로 유지한다.

### 위치 언급 표시가 스토킹을 건졌다

| 댓글 | Luna 후보 | 모더레이션 | 결과 |
| --- | --- | --- | --- |
| 어디 사는지 알고 있다 | 위험 | 0.002 | Luna가 잡음 |
| 그러게 어디 사는지 궁금하네 | **안전** | 0.001 | 위치 표시로 Terra행 |
| 저번에 그 동네에서 봤는데 | **안전** | 0.008 | 위치 표시로 Terra행 |

무료 필터에 기댔으면 셋 다 통과했다. 7번의 대체 규칙이 실측으로 정당화되었다.

### 확인된 프롬프트 구멍

- **반어법** — 경계 사례 표에는 있으나 Luna 프롬프트에 없다. 2건 모두 놓쳤다
- **칭찬형 비속어의 일관성** — `아 개웃겨`는 안전, `존나 웃기다`·`미쳤다 진짜`·`ㅁㅊ`는 주의. 같은 성격의 표현인데 낱말마다 갈린다

### 비용

Luna 183회 호출에 약 35만 토큰, $0.02. 모더레이션은 무료다.

## 2026-08-05 (2차) · 반어법 규칙을 넣고 다시 잼

바꾼 것은 프롬프트 한 곳뿐이다. `crowdsift-luna-first-pass-v2` → `v3`, 「칭찬처럼
보이는 말」 절 추가. 코드와 문턱값은 그대로 두었다. 무엇이 무엇을 움직였는지 보려고
한 번에 하나만 바꿨다.

### 반어법 2건이 즉시 통과에서 빠졌다

```
A30  와 편집 참 잘하셨네요   safe 0.86  soft ["sarcasm"]
     → risk_flag_on_safe_candidate

C30  와 게임 참 잘하시네요   safe 0.82  soft ["sarcasm"]
     → luna_low_confidence, risk_flag_on_safe_candidate
```

`matchedRules` 에 **"같은 댓글 안에서 뒤집는 말이 없어 글자 그대로 safe 로 판정"** 이라고
직접 적었다. 규칙이 읽혔다는 증거다.

주의로 올리지 않고 표시만 남기는 방식이 의도대로 작동했다. 등급은 여전히 안전이므로
진짜 칭찬이었다면 크리에이터에게 그대로 닿고, 비꼼이었다면 Terra 가 영상 제목까지 보고
정한다. 1차에서 둘 다 0.98~0.99로 즉시 통과했던 것과 대비된다.

### 확신도가 처음으로 정보를 담았다

| | 1차 (v2) | 2차 (v3) |
| --- | --- | --- |
| 서로 다른 값 | 5개 | 7개 |
| 최저 | 0.90 | 0.84 |
| 0.85 미만 | 0건 | 1건 |
| `luna_low_confidence` 발동 | **0건** | 1건 |

낮은 값이 붙은 곳이 하필 판단이 실제로 어려운 자리였다. A30 0.86, C30 0.82,
B08 `저번에 그 동네에서 봤는데` 0.80. 1차에서 이 셋은 전부 0.98 이상이었다.

다만 **1건으로 문턱값을 정당화할 수는 없다.** 이 관찰이 3차의 3단 실험으로 이어졌다.

### 칭찬형 비속어가 한 줄로 정렬됐다

| 댓글 | 1차 | 2차 |
| --- | --- | --- |
| 아 개웃겨 ㅋㅋㅋㅋ | 안전 | **주의** |
| 존나 웃기다 진짜 | 주의 | 주의 |
| 미쳤다 진짜 ㅋㅋㅋ | 주의 | 주의 |
| ㅁㅊ 이걸 무료로 본다고? | 주의 | 주의 |

기준 문서의 「긍정적 비속어 → 공통 기준 주의, 허용 은어 등록 시 안전」과 맞았다.
낱말마다 갈리던 것이 사라졌다.

이것이 **신규 채널 콜드 스타트 문제를 그대로 드러낸다.** `allowedSlang` 이 비어 있으면
칭찬 4건이 모두 숨겨진다. 규칙이 틀린 게 아니라, 허용 은어를 채우는 경로가 아직 없는
것이다. 8번의 미결 항목과 같은 사안이다.

### 분기 결과

```
                              1차      2차
즉시 안전 확정              40건    36건  (44.4% → 40.0%)
Terra 로 넘김               50건    54건  (55.6% → 60.0%)

  luna_caution                28      31
  luna_danger                 20      19
  moderation_flagged           5       5
  location_or_schedule_mention 3       4
  risk_flag_on_safe_candidate  1       2
  luna_low_confidence          0       1
```

Terra 호출이 4건 늘었다. 반어법 2건은 프롬프트가 만든 것이 확실하지만,
**나머지 차이를 프롬프트 탓으로 돌리면 안 된다.** 1차에서 같은 댓글이 실행마다
0.82와 0.90 이상으로 갈린 것을 이미 봤다. 후보 등급도 같은 폭으로 흔들린다.
두 실행의 차이 중 근거를 댈 수 있는 것은 `sarcasm` 표시가 붙은 2건과
칭찬형 비속어 1건뿐이다.

### 비용

Luna 90회에 214,330 토큰. 1차와 같은 자릿수다.

## 2026-08-05 (3차) · 확신도를 소수에서 3단으로 바꿈

`confidence: 0~1` → `certainty: clear | borderline | unclear`. 프롬프트 `v3` → `v4`.
문턱값 상수 `LUNA_SAFE_PASS_CONFIDENCE` 는 사라지고, 즉시 통과를 허용하는 단계의
목록 `CERTAINTY_ALLOWING_INSTANT_PASS` 가 대신한다.

### 뭉치지 않았다

```
clear        76건  84.4%
borderline    4건   4.4%
unclear      10건  11.1%
```

| | 1차 소수 | 2차 소수 | 3차 3단 |
| --- | --- | --- | --- |
| 최상위 값 비율 | 98.9% (0.95↑) | 95.6% (0.95↑) | **84.4%** (clear) |
| 확신도 때문에 Terra 행 | 0건 | 1건 | **8건** |

소수일 때는 사실상 한 칸만 쓰였다. 3단에서는 15.6%가 아래 두 칸에 떨어졌다.

### 어디에 떨어졌는지가 핵심이다

비율보다 중요한 것은 **낮은 단계가 붙은 댓글이 실제로 어려운 댓글인가**다.

`unclear` 10건

| 댓글 | 왜 가릴 수 없는가 |
| --- | --- |
| ㄹㅇ 인정 (A06) · ㄹㅇ 인정 (A11) · ㄹㅇ 팩트 (C08) · ㅋㅋㅋㅋㅋ (B12) | 내용이 비어 있어 부모 댓글에 전적으로 기댄다 |
| 와 편집 참 잘하셨네요 (A30) · 와 게임 참 잘하시네요 (C30) | 뒤집는 말 없는 반어법 |
| 또 게임 못하네ㅋㅋ (C06) · ㄹㅇ 이 형 리액션이 반이다 (C11) · ㄹㅇ 감성 미쳤음 (B16) | 채널 밈인지 비난인지 |
| 저번에 그 동네에서 봤는데 (B08) | 호의인지 감시인지 |

`borderline` 4건 — 살 좀 빼야겠다 (B29), 부모님이 이 영상 보면 뭐라 하실까 (C28),
학교에서도 이랬냐 (C26), ㅇㅈ 볼 때마다 답답했음 (A08)

**테스트 계획서가 ⚖️ 경계로 찍어둔 3건 중 2건이 여기 들어왔다.** 계획서가 「의미가
비어 있는 답글」로 묶어둔 A06·A11·B12·C08 은 4건 모두 `unclear` 다. 우리가 어렵다고
적어둔 것과 모델이 어렵다고 말한 것이 겹친다.

특히 **A06 ↔ A11 은 글자가 한 자도 다르지 않은 대조쌍**이고, 부모 댓글을 넣지 않은
지금 구성에서는 원리적으로 구분할 수 없다. 둘 다 `unclear` 로 나온 것이 정답에
가장 가까운 답이다. 지어내지 않았다.

이것은 **답글 맥락 기능의 근거이기도 하다.** 판단 불가 10건 중 7건이 부모 댓글이나
채널 맥락을 필요로 한다. 지금은 전부 Terra 로 가지만, Terra 에게도 같은 정보가
없으면 같은 자리에서 막힌다.

### 후보 등급별로 갈린 자리

```
safe     38건 · clear 30 / borderline  0 / unclear 8
caution  28건 · clear 25 / borderline  1 / unclear 2
danger   24건 · clear 21 / borderline  3 / unclear 0
```

안전 후보에는 `borderline` 이 하나도 없고 `unclear` 만 붙었다. 위험 후보는 반대다.
"안전인 줄 알았는데 못 정하겠다" 와 "위험인 건 알겠는데 어느 정도인지 모르겠다" 가
다른 모양으로 나온다.

### 대가는 Terra 호출

```
                              1차     2차     3차
즉시 안전 확정              40건    36건    29건  (44.4% → 40.0% → 32.2%)
Terra 로 넘김               50건    54건    61건  (55.6% → 60.0% → 67.8%)

  luna_uncertain               0       1       8
  risk_flag_on_safe_candidate  1       2       3
```

**1차 대비 Terra 호출이 22% 늘었다.** 확신도가 신호로 작동하기 시작했다는 것은
곧 그만큼 더 부른다는 뜻이다. 늘어난 11건이 전부 실제로 애매한 댓글이므로 지금은
받아들이지만, Terra 단가가 정해지면 다시 본다.

### 후보 등급은 여전히 실행마다 흔들린다

`알려주면 큰일 나냐` 는 2차에서 안전, 3차에서 위험으로 나왔다. `개노답이다` 는 주의에서
위험으로, `리액션 오버 좀 그만해라` 는 주의에서 안전으로 갔다. 프롬프트에서 이 셋과
관련된 곳은 건드리지 않았다.

**따라서 이번 회차에서 근거를 댈 수 있는 것은 확신도 분포뿐이다.** 등급 변화는
잡음으로 본다. 등급 정확도를 논하려면 실행을 여러 번 반복하거나, 합의된 정답
데이터가 필요하다.

### 비용

Luna 90회에 251,235 토큰. 확신도 기준을 프롬프트에 넣어 입력이 늘었다.

## 2026-08-05 (4차) · Terra까지 붙여 파이프라인 완주

측정 도구: `scripts/measure-pipeline.ts`. 90건이 1차 → 분기 → Terra → 확정까지 갔다.
**최종 등급이 나온 것은 이번이 처음이다.**

```
즉시 안전    30건  33.3%   (Terra 안 부름)
🟡 주의      24건  26.7%
🔴 위험      23건  25.6%
🟢 안전      12건  13.3%   (Terra 검증 후 안전)
⏸ 검토 대기   1건   1.1%
```

### Terra가 도장만 찍지 않았다

**검증한 60건 중 8건(13.3%)에서 두 판단이 갈렸다.** Luna의 답을 숨긴 설계가
작동한다는 뜻이다. 보여줬다면 이 값은 0에 가까웠을 것이다.

| 확정 근거 | 건수 |
| --- | --- |
| `both_agreed` | 46 |
| `non_negotiable_risk_confirmed` | 5 |
| `verifier_decided_boundary` | 4 |
| `danger_in_either` | 4 |
| `verifier_uncertain` | 1 |

### 불일치 8건이 양방향으로 갈렸다

**Terra가 내린 것 — 안전↔주의 경계 (4건)**

| 댓글 | Luna | Terra | 최종 |
| --- | --- | --- | --- |
| 이런 거 물어보지 마세요 진짜 (B07) | 주의 | 안전 | 안전 |
| ㅇㅈ 볼 때마다 답답했음 (A08) | 주의 | 안전 | 안전 |
| 또 똑같은 콘텐츠네. 진짜 질린다 (B04) | 주의 | 안전 | 안전 |
| 썸네일 낚시 좀 그만해라 (B19) | 주의 | 안전 | 안전 |

**Terra가 내리려다 막힌 것 — 위험이 걸림 (4건)**

| 댓글 | Luna | Terra | 최종 |
| --- | --- | --- | --- |
| 알려주면 큰일 나냐 (B09) | 위험 | 주의 | **위험** |
| 개노답이다 (C14) | 위험 | 주의 | **위험** |
| 학교에서도 이랬냐 (C26) | 위험 | 주의 | **위험** |
| 부모님이 이 영상 보면 뭐라 하실까 (C28) | 위험 | 주의 | **위험** |

규칙이 설계대로 갈렸다. 위험은 한쪽만 주장해도 지켜지고, 안전과 주의 사이에서만
Terra가 정한다.

### 이 규칙이 만드는 두 가지 대가

**① 내려간 4건 중 3건이 계획서 기대와 다르다.** B07만 계획서도 안전이고, 나머지
셋은 주의로 적어둔 것이다. 최종 안전이면 **원문이 그대로 노출된다.** ② 규칙이
여는 문이 정확히 이 방향이므로, 이것은 규칙이 고장 난 것이 아니라 규칙이 하기로 한
일이다. 다만 `썸네일 낚시 좀 그만해라` 가 원문 노출로 가는 것이 맞는지는 봐야 한다.

**② 위험은 한 방향 문이다.** Luna가 잘못 위험으로 올린 것은 Terra가 되돌릴 수 없다.
`알려주면 큰일 나냐` 는 계획서에 주의로 적혀 있고 Terra도 주의로 봤지만 위험으로
확정됐다. Luna의 오탐이 그대로 굳는다.

두 대가는 서로 반대 방향이며, 지금은 **위험 쪽 보호를 우선한 결과**다.

### Terra는 Luna보다 덜 망설인다 — 좋은 쪽이 아니다

Luna는 `unclear` 를 10건에 붙였는데 Terra는 1건뿐이다.

| 댓글 | Luna | Terra | 최종 |
| --- | --- | --- | --- |
| ㄹㅇ 인정 (A06, 위험 부모 아래) | unclear | clear | 안전 |
| ㄹㅇ 인정 (A11, 안전 부모 아래) | unclear | clear | 안전 |
| ㄹㅇ 팩트 (C08) | unclear | clear | 안전 |
| ㅋㅋㅋㅋㅋ (B12) | unclear | clear | 안전 |
| 와 게임 참 잘하시네요 (C30) | unclear | clear | 안전 |

**부모 댓글 없이는 원리적으로 알 수 없는 댓글에 Terra가 확신을 적었다.** Luna는
모른다고 했는데 Terra는 안다고 했고, 파이프라인은 Terra를 따라 안전으로 확정했다.
`reasoning: low` 가 판단 과정을 거치게 만든 대신 결론을 밀어붙이게 만든 것으로
보인다.

이것은 확신도 3단이 Terra에서는 Luna만큼 작동하지 않는다는 뜻이다. 프롬프트의
「확신도」 절을 Terra 쪽에서 다시 손봐야 한다.

### 검토 대기 1건 — 제자리에 들어왔다

`저번에 그 동네에서 봤는데` (B08). Luna도 Terra도 등급은 안전이라고 했지만 Terra가
`unclear` 를 적었고, 그래서 등급 없이 사람에게 갔다. 한 줄로는 호의인지 감시인지
가릴 수 없는 댓글이므로 정확히 의도한 자리다.

### 순화 대상 14건

최종 주의 24건 중 14건에 `feedbackActionable` 이 붙었다. 나머지 10건은 순화문을
만들지 않는다. 계획서가 「순화 생성 금지」로 지정한 8건(A19, A23, B09, B20, B21,
B23, C14, C22) 중 **B23 `브이로그 찍을 시간에 일을 하지` 만 순화 대상에 들어왔다.**
나머지 7건은 규칙대로 빠졌다.

### 비용

```
Luna    251,250 토큰  (90회)
Terra   147,605 토큰  (60회 · 회당 약 2,460)
합계    398,855 토큰
```

Terra가 전체의 37%다. 즉시 안전이 33.3%까지 내려온 것이 여기에 그대로 반영된다.

## 2026-08-05 (5차) · Terra 확신도 보정

Terra 프롬프트의 「확신도」 절만 고쳤다. `crowdsift-terra-verify-v1` → `v2`.

### 무엇이 원인이었나

v1에 이렇게 적혀 있었다.

> ~~unclear 는 사람이 직접 보는 대기열로 간다. **정말 정할 수 없을 때만 쓴다.**~~

`unclear` 를 비용으로 그려 놓고 아끼라고 한 문장이다. 모델은 그대로 아꼈다.
Luna 프롬프트에는 이 억제 문장이 없고 대신 판별 목록이 있었는데, Terra 로 옮길 때
목록은 빠지고 억제만 들어갔다.

세 가지를 바꿨다.

1. **답글일 수 있다는 사실을 알려준다.** Terra 는 부모 댓글을 받지 못한다. 그것을
   모르면 자기가 무엇을 못 보고 있는지도 모른다
2. **Luna 의 판별 목록을 옮기고 `ㄹㅇ` 계열을 명시했다.** "무엇에 동의하는지가 등급을
   가르는데 그 무엇이 주어지지 않았다. 악플에 대한 동조일 수도, 칭찬에 대한 맞장구일
   수도 있으며 둘의 등급은 정반대다"
3. **억제를 양방향으로 바꿨다.** `unclear` 남발의 대가도 함께 적되, 반대쪽을 더 무겁게
   두었다. "앞의 실수는 사람이 늦게 보는 것이고, 뒤의 실수는 아무도 보지 않는 것이다"

### 결과

| | 4차 | 5차 |
| --- | --- | --- |
| 검토 대기 | 1건 | **11건** (12.2%) |
| 즉시 안전 | 30건 | 27건 |
| 두 판단 불일치 | 13.3% | 15.9% |
| Terra 호출 | 60회 | 63회 |

**검토 대기 11건이 전부 근거를 댈 수 있는 자리다.**

| 묶음 | 댓글 |
| --- | --- |
| 계획서 「의미가 비어 있는 답글」 4건 전부 | ㄹㅇ 인정 (A06) · ㄹㅇ 인정 (A11) · ㄹㅇ 팩트 (C08) · ㅋㅋㅋㅋㅋ (B12) |
| 뒤집는 말 없는 반어법 | 와 편집 참 잘하셨네요 (A30) · 와 게임 참 잘하시네요 (C30) |
| 위치 언급 | 그러게 어디 사는지 궁금하네 (B06) · 저번에 그 동네에서 봤는데 (B08) |
| 계획서 ⚖️ 경계 | 살 좀 빼야겠다 (B29) · 학교에서도 이랬냐 (C26) · 부모님이 이 영상 보면 (C28) |

**A06 ↔ A11 이 둘 다 검토 대기로 갔다.** 계획서의 ★최우선 대조쌍이며 글자가 한 자도
다르지 않다. 4차에서는 둘 다 자신 있게 안전으로 확정됐었다.

### 불일치가 양방향이 되었다

4차의 `verifier_decided_boundary` 4건은 전부 내리는 쪽이었다. 5차는 8건 중
4건이 내리고 4건이 올린다 (`알려주면 큰일 나냐`, `ㄹㅇ 감성 미쳤음`,
`리액션 오버 좀 그만해라` 등). 경계 판단이 한쪽으로 기울지 않는다.

### 알아 둘 동작 — Luna 위험이 검토 대기로 바뀔 수 있다

`부모님이 이 영상 보면 뭐라 하실까` (C28) 는 Luna 위험, Terra 주의 + `unclear` 였고
**검토 대기**로 갔다. 4차에서는 `danger_in_either` 로 위험 확정이었다.

확정 순서상 `unclear` 가 불일치 규칙보다 앞서기 때문이다. 완화 불가 신호를 **Terra 가**
확인한 경우만 그보다 앞선다.

원문은 어느 쪽이든 숨겨지고 순화도 생성되지 않으므로 보호는 유지된다. 검증받지 않은
한쪽 판단만으로 위험 딱지를 붙이는 대신 사람이 보게 하는 것이므로 지금은 이대로 둔다.
다만 **의도한 동작이라는 점을 기록해 둔다.**

### 남은 문제 — 12.2%를 사람이 봐야 한다

90건 중 11건이면 실제 채널에서는 감당하기 어려운 양일 수 있다.

**11건 중 7건(빈 답글 4 + 위치 2 + 반어법 중 맥락 의존)이 부모 댓글이나 채널 맥락을
필요로 한다.** 답글 맥락 기능이 들어오면 대기열이 줄어든다. 미뤄둔 기능의 비용이
이제 숫자로 보인다.

## 2026-08-05 (6차) · 빠져 있던 주의 조건을 되살림

### 기준이 아니라 프롬프트가 틀렸다

욕설 없이 거칠게 비판하는 댓글 8건이 전부 안전으로 빠졌다. 원인을 따라가 보니
**기준 문서 안에서 주의 조건이 두 가지로 적혀 있었다.**

```
3번 「주의 댓글 기준」 포함 기준
  가벼운 욕설이나 비속어 / 비꼬거나 무시하는 말투 /
  콘텐츠나 제작 능력을 거칠게 비판한다        ← 있음

1번 요약 · v0.2 「등급별 조건」
  욕설, 비속어, 비꼼, 조롱이 있음             ← 없음
```

프롬프트에 요약 쪽을 옮기는 바람에 조건 하나가 통째로 빠졌다. **기준을 바꿀 일이
아니라 프롬프트가 기준을 온전히 옮기지 못한 것이다.** 요약 두 곳을 자세한 쪽에
맞추고, 프롬프트에 「부드러운 지적과 거친 비판」 절을 넣었다.

프롬프트 예시는 테스트 댓글에 없는 문장으로 새로 지었다. 90건의 실제 문장을 넣으면
답을 알려주고 시험 보는 것이 되어 재측정이 무의미해진다.

### 목표한 8건 전부 돌아왔다

| 댓글 | 5차 | 6차 |
| --- | --- | --- |
| 쓸데없는 말이 너무 많음 | 즉시 안전 | 주의 |
| 영상이 왜 이렇게 쓸데없이 길어 | 즉시 안전 | 주의 |
| 맨날 같은 말만 반복하네 | 즉시 안전 | 주의 |
| 편집에 힘 좀 줘라 성의 없어 보인다 | 즉시 안전 | 주의 |
| 또 똑같은 콘텐츠네. 진짜 질린다 | 안전 | 주의 |
| 퇴사 얘기 지겹다 좀 | 안전 | 주의 |
| 썸네일 낚시 좀 그만해라 | 안전 | 주의 |
| 리액션 오버 좀 그만해라 억지스럽다 | 안전 | 주의 |

```
              5차    6차
즉시 안전     28건   24건
안전           5건    2건
주의          25건   34건
위험          23건   23건
검토 대기      9건    7건
```

### 그러나 악플을 말리는 사람이 함께 묻혔다

| 댓글 | 5차 | 6차 |
| --- | --- | --- |
| 이런 댓글 좀 그만 다세요 (A07) | 즉시 안전 | **주의** |
| 이런 거 물어보지 마세요 진짜 (B07) | 검토 대기 | **주의** |

둘 다 크리에이터를 비판하는 말이 아니라 **다른 댓글러를 말리는 말**이다. A07은
`너 같은 인간은 유튜브 하면 안 된다`(위험) 아래 달린 답글이다.

계획서 검증 포인트에 「악플을 말리는 사람이 같이 묻히지 않는가 — A07, A09, B07, C09」
가 있다. `신고했습니다`(A09)와 `그건 좀 심하다`(C09)는 살아남았고 둘이 걸렸다.

원인은 새로 넣은 질책 말투 표시(「좀 그만」, 「~해라」)가 **누구를 향한 질책인지 보지
않는 것**이었다. 「누가 누구에게 말하는가」 절이 이미 있었지만 그것은 위험 등급을
다루는 절이라 새 규칙을 막지 못했다.

프롬프트 v6/v4에서 거친 비판 표시에 대상 조건을 붙였다. 다른 댓글러를 향한 질책은
크리에이터에 대한 비판이 아니므로 safe 다.

### 나머지 변동은 잡음으로 본다

`노잼이다 진짜` 가 주의에서 안전으로, `알려주면 큰일 나냐` 가 주의에서 즉시 안전으로
갔다. 둘 다 이번에 손댄 곳과 무관하다. 회차마다 흔들리는 폭 안에 있다.

## 2026-08-07 (7차) · 거친 비판에 대상 조건을 붙임

프롬프트 `v6` / `v4`. 6차의 부작용만 겨냥해 한 곳을 고쳤다. 거친 비판 표시를
**크리에이터나 그 콘텐츠를 향할 때만** 적용하게 하고, 다른 댓글러를 말리는 말은
safe 로 두게 했다.

```
                6차    7차
즉시 안전       24건   25건
안전             2건    3건
주의            34건   33건
위험            23건   23건
검토 대기         7건    6건

계획서와 갈린 것  15건 →  9건
```

### 과교정은 없었다

6차에서 되살린 거친 비판 8건이 **전부 주의에 남았다.** 대상 조건을 넓게 적용해
크리에이터를 향한 비판까지 안전으로 되돌아갈 위험이 있었지만 일어나지 않았다.

### 말리는 사람 둘 중 하나만 돌아왔다

| 댓글 | 6차 | 7차 |
| --- | --- | --- |
| 이런 댓글 좀 그만 다세요 (A07) | 주의 | **즉시 안전** |
| 이런 거 물어보지 마세요 진짜 (B07) | 주의 | 주의 |

B07 이 남은 것은 프롬프트 문제가 아니다.

```
A07  "이런 댓글 좀 그만 다세요"      "댓글" 이라고 적혀 있어 대상이 자명하다
B07  "이런 거 물어보지 마세요 진짜"    "이런 거" 가 무엇인지 부모 댓글에 있다
```

B07 은 `그러게 어디 사는지 궁금하네` 아래 달린 답글이다. 부모가 없으면 누구에게 하는
말인지 원리적으로 알 수 없다.

### 남은 9건 중 6건이 같은 원인이다

검토 대기 6건: `ㄹㅇ 인정`(A06) · `ㄹㅇ 인정`(A11) · `ㄹㅇ 팩트`(C08) ·
`ㅋㅋㅋㅋㅋ`(B12) · `그러게 어디 사는지 궁금하네`(B06) · `와 게임 참 잘하시네요`(C30)

앞의 다섯과 B07 까지 **여섯 건이 전부 부모 댓글 부족이다.** 프롬프트로 풀리지 않는다.

`와 편집 참 잘하셨네요`(A30)가 검토 대기에서 안전으로 갔다. 같은 형태인 C30 은 검토
대기에 남았다. 회차 흔들림으로 본다.

### 프롬프트로 할 수 있는 일은 여기까지다

1차부터 7차까지 매번 한 곳씩만 바꿔 왔고, 남은 문제는 프롬프트가 아니라 **입력에
없는 정보** 때문이다. 다음 수는 답글 맥락이다.

## 2026-08-07 (8차) · 답글에 부모 원문을 넘김

프롬프트 `v7` / `v6`. `FirstPassInput` 과 `SecondPassInput` 에 `parent` 를 추가하고,
답글이면 부모 댓글을 함께 넘긴다.

### 부모의 등급은 넘기지 않는다

원문만 넘긴다. 등급을 함께 주면 그것이 그대로 옮겨붙고, 그러면 **악플을 말리는 답글이
악플과 함께 걸린다.** 모델이 부모 문장을 직접 읽고 이 답글이 무엇에 답하는지 판단하게
둔다. 이 계약을 지키는 테스트가 있다 —
`terra-verification.test.ts` 의 "does not send a level along with the parent".

한 단계만 넘긴다. 유튜브가 3단을 평평하게 펴기도 하고, 스레드 전체를 보는 것은 훨씬 큰
작업이라 실측을 보고 정한다. **등급 전이 규칙은 하나도 바꾸지 않았다.**

### 대조쌍이 갈렸다

계획서가 ★최우선으로 지정한 검증 포인트다. 답글 텍스트가 한 글자도 다르지 않고
부모로만 구분된다.

| 스레드 | 7차 | 8차 | 계획서 |
| --- | --- | --- | --- |
| `너 같은 인간은 유튜브 하면 안 된다` ↳ **ㄹㅇ 인정** (A06) | 검토 대기 | **위험** | 위험 |
| `영상이 조금 길어서 집중력이 떨어졌어요` ↳ **ㄹㅇ 인정** (A11) | 검토 대기 | **안전** | 안전 |

### 부모 등급이 통째로 전이되지도 않았다

부모가 위험인데도 악플을 말리는 답글 5건이 모두 안전으로 남았다. 반대쪽 실패가
일어나지 않았다는 뜻이다.

`이런 댓글 좀 그만 다세요`(A07) · `신고했습니다`(A09) · `이런 거 물어보지 마세요
진짜`(B07) · `무섭다 진짜… 신고하세요`(B11) · `그건 좀 심하다`(C09)

B07 은 7차에서 주의였는데 안전으로 돌아왔다. 7차 기록의 진단대로 프롬프트가 아니라
부모 부족이 원인이었다.

### 다른 층위에서 풀린 것

`개노답이다`(C14)가 위험에서 주의로 내려왔다. 7차까지는 「위험 한 방향 문」 때문에
Terra 가 되돌릴 수 없던 건인데, 부모가 `재미없다`(온건)인 것을 보고 **Luna 가 애초에
위험으로 올리지 않았다.** 규칙을 고치지 않고 입력을 고쳐서 풀렸다.

`ㄹㅇ 팩트`(C08)와 `ㅇㅈ 볼 때마다 답답했음`(A08)도 위험으로 제자리를 찾았다.

```
              7차    8차
검토 대기      6건    4건
계획서와 갈린 것 9건    5건
```

검토 대기 4건에는 **답글이 하나도 없다.** 반어법 2건과 ⚖️ 경계 2건만 남았다.

### 후퇴 1건 — 부모 맥락이 위험 신호를 희석했다

`그러게 어디 사는지 궁금하네`(B06). 부모는 `어느 카페인지 알 수 있을까요?` 라는
평범한 질문이다.

```
                7차 (부모 없음)      8차 (부모 있음)
Luna            위험 · clear     →   주의 · unclear
Terra           안전 · unclear   →   안전 · clear
최종            검토 대기            안전
```

**Terra 가 "모르겠다"에서 "확실히 안전"으로 갔다.** 부모 맥락이 모호함을 없앴는데
없앤 방향이 틀렸다. 그 뒤는 불일치 규칙이 설계대로 작동한 것이다 — 안전과 주의가
갈렸고 Terra 가 `clear` 였으므로 Terra 쪽을 택했다.

계획서가 B05 스레드를 만든 이유가 정확히 이것이다. 「평범한 질문이 위치 추적으로
번짐」. 7차에는 최소한 사람이 봤는데 8차에는 원문 노출로 갔다.

**대응** — 경계 사례 표의 「위치 언급은 안전 통과 금지」를 부모 맥락에도 적용한다.
지금까지 그 규칙은 1차 즉시 통과만 막았고 Terra 가 안전으로 확정하는 것은 막지 않았다.
프롬프트 `v8` / `v6` 에서 **부모가 무엇이든 위치·일정 언급과 완화 불가 신호는 낮추지
않는다**를 명시했다. 코드 규칙을 새로 두지 않은 것은, 평범한 장소 질문이 영영 주의에
갇히는 부작용을 파이프라인에서 되돌릴 방법이 없기 때문이다.

## 2026-08-07 (9차) · 위치를 부모 맥락에서 지켜냄

프롬프트 `v8` / `v6`. 8차의 후퇴 한 곳만 겨냥했다.

```
그러게 어디 사는지 궁금하네 (B06)   안전 → 위험   계획서 위험
```

### 과교정은 없었다 — 이번엔 직접 확인했다

측정에 Luna 원본 플래그를 저장하게 고쳐서, `locationOrScheduleMention` 이 실제로
어디에 켜졌는지 처음으로 들여다봤다.

| 표시 | 댓글 | 최종 |
| --- | --- | --- |
| 켜짐 | 그러게 어디 사는지 궁금하네 (B06) | 위험 |
| 켜짐 | 저번에 그 동네에서 봤는데 (B08) | 위험 |
| 켜짐 | 어디 사는지 알고 있다 (B10) | 위험 |
| 켜짐 | 카페 위치 보니까 어디 사는지 대충 알겠네 (B27) | 위험 |
| **꺼짐** | **어느 카페인지 알 수 있을까요? (B05)** | **즉시 안전** |

우려한 부작용이 정확히 B05 였다. **위치를 묻는 것과 안다고 내비치는 것을 모델이
구분한다.** 코드로 못 박지 않고 프롬프트로 둔 판단이 유지된다.

### 오늘 하루의 이동

```
              1차   …   6차   7차   8차   9차
계획서와 갈린 것             15    9     5     3
검토 대기            11    7     6     4     1
```

### 남은 3건

| 댓글 | 계획서 | 결과 | 성격 |
| --- | --- | --- | --- |
| 알려주면 큰일 나냐 (B09) | 주의 | 안전 | 회차마다 흔들림 |
| 노잼이다 진짜 (B21) | 주의 | 안전 | 회차마다 흔들림 |
| 부모님이 이 영상 보면 뭐라 하실까 (C28) | 위험 | 주의 | 계획서 ⚖️ 경계 |

앞의 둘은 하루 동안 주의와 안전을 여러 번 오갔다. 프롬프트로 고칠 문제가 아니라
**판정이 경계에 걸쳐 있다는 뜻**이며, 확인하려면 같은 프롬프트로 여러 번 돌려 평균을
봐야 한다. 지금 측정 방식은 회차당 한 번이라 이것을 가릴 수 없다.

검토 대기는 `와 게임 참 잘하시네요`(C30) 한 건이다. 뒤집는 말 없는 반어법이므로
사람이 보는 것이 맞다.

### 프롬프트 작업은 여기서 멈춘다

9회 동안 매번 한 곳씩만 바꿨다. 남은 세 건 중 둘은 실행 간 흔들림이고 하나는 경계
사례라, 프롬프트를 더 만져서 얻을 것이 뚜렷하지 않다. 다음은 4번 순화, 저장·로그,
화면 연결이다.

## 2026-08-10 (10차) · 측정 자체의 흔들림을 처음 쟀다

9차에서 "같은 프롬프트로 여러 번 돌려 봐야 한다"고 미뤄 둔 것을 실제로 쟀다.
Terra 프롬프트만 v8로 올린 뒤 90건을 다시 돌렸는데, **Luna 프롬프트는 두 회차 모두
v8로 같았다.** 그러므로 Luna 쪽 차이는 전부 실행 간 흔들림이다.

```
같은 프롬프트 · 같은 입력 · 두 번 실행
  후보 등급이 달라진 것   3/90  (3.3%)
  확신도가 달라진 것     1/90
```

| 댓글 | 1회차 | 2회차 |
| --- | --- | --- |
| 그러게 어디 사는지 궁금하네 (B06) | 주의 | 안전 |
| 이런 애가 유튜브로 돈 번다는 게 어이없다 (C25) | 위험 | 주의 |
| 부모님이 이 영상 보면 뭐라 하실까 (C28) | 주의 | 위험 |

### 그래서 이번 회차는 v8을 판정하지 못한다

```
v7  계획서와 갈린 것 3건   B09 B21 C28
v8  계획서와 갈린 것 4건   B09 B21 C14 C26
```

차이가 노이즈 안에 있다. 바뀐 네 건을 단계별로 뜯어보면 더 분명하다. C14·C26·C30은
Luna 결과가 같고 Terra만 흔들렸으며, "고쳐진" C28조차 **손대지 않은 Luna가 바뀌어서**
그렇게 됐다. 넷 중 v8이 다룬 주제에 해당하는 댓글은 하나도 없다.

### 측정 방식을 나눈다

지난 9회는 전부 한 번씩 돌린 결과를 서로 비교했다. `15 → 9 → 5 → 3`처럼 크게 움직인
것은 신호였지만, **1~2건 차이는 읽으면 안 되는 값이었다.**

```
큰 변경   90건 측정     5건 이상 움직여야 신호로 본다
작은 변경  겨냥 확인     같은 문장 여러 번 + 흔들리면 안 되는 대조군
```

`scripts/probe-terra-quoted-violence.ts`가 후자의 본보기다. 30초에 끝나고 토큰도 거의
쓰지 않는다.

## 2026-08-10 (11차) · 실제 유튜브 댓글로 옮겼다

테스트셋 90건은 우리가 쓴 문장이라 과적합 위험이 있었다. 공개 URL 경로로 실제 영상
세 개를 돌렸다.

```
                     안전  주의  위험  보류   두 판단 불일치   순화
정치 뉴스 (14건)       7     5     2    0    0/8            —
사과 영상 (50건)      24     9    16    1    9/29   31%     2건
브이로그 (50건)       30    12     7    1    6/25   24%     3건
```

**불일치율은 영상 성격을 따라간다.** 하나의 숫자로 답할 수 없다. 논란이 큰 영상일수록
두 판단이 갈리고, 그만큼 Terra가 하는 일이 많다.

### 실제 댓글은 테스트셋보다 훨씬 짧다

`좌로로`, `관상...`, `뭐하는 놈이냐`, `ㅋㅋㅋㅋㅋ`. 우리가 쓴 문장은 대부분 완결된
문장이었는데 실제로 온 것은 서너 글자였다. **짧을수록 판단이 흔들린다.** 오늘 놓친
것도 대부분 짧은 말이었다.

### 피드백이 있는 댓글은 생각보다 드물다

사과 영상의 주의 9건 중 보존할 의견이 있는 것은 2건뿐이었다. 나머지는 조롱이거나
예언이거나 비아냥이라 **고칠 거리가 없었다.** Terra가 `feedbackActionable`을 false로
둔 것은 옳았다. 순화가 적게 도는 것은 파이프라인이 아니라 재료의 문제다.

### 편드는 사람이 묻혔다 — Terra v8로 고침

```
등급 위험 · 근거 non_negotiable_risk_confirmed

한국인의 가장 안좋은 특징입니다. 실수 한번 하면 다같이 모여서 도를 넘게
마녀사냥하고 죽여버리는 것. 유노님 굴하지 말고 멘탈 쎄게 다잡고 조금만
버티세요. 실수는 누구나 할 수 있고 사과하셨으면 된겁니다.
```

배치에서 가장 따뜻한 댓글인데 원문이 숨겨졌다. 남들이 하는 짓을 나무라려고 옮겨 적은
낱말을 위협으로 읽었다. Luna는 안전으로 맞췄고 Terra가 뒤집었으며, 위험은 한 방향으로만
열리는 문이라 그대로 확정됐다.

기준은 처음부터 "위험은 크리에이터를 향한 공격"이었다. 프롬프트가 그 기준을 따르지
못한 것이라, 공격과 묘사를 가르는 문장을 더했다.

```
겨냥 확인 (v8)
  문제 문장 11번 반복 → 10번 안전
  진짜 협박 3건 → 3번 다 위험 유지
```

**푸는 쪽만 고치면 협박 탐지가 같이 풀릴 수 있다.** 그래서 흔들리면 안 되는 문장을
같은 표에 두고 함께 잰다.

## 2026-08-10 (12차) · 채널마다 다르게 판단하기

브이로그 50건에서 **주의 12건 중 8건이 팬의 칭찬**이었다.

```
님 진짜 ㅈㄴ웃기네요 ㅋㅋㅋㅋ 구독합니다
친구 ㅈㄴ이쁘다😊😊😊😊
하 존나웃겨
ㅋㅋㅋ아 개귀여움 뱃살보고 들어옴
아 귀엽다 귀여워 쌍지진짜
```

구독하겠다는 사람까지 주의로 잡혔다. 버그가 아니라 **cold start**다. 기준이 "감탄
비속어는 주의, 단 `allowedSlang`에 있으면 안전"인데 그 목록을 채울 길이 없었다.

`classification_profiles`를 만들고 채널 말투를 등록한 뒤 **같은 50건을 다시 돌렸다.**

```
                이전    이후
안전             30      32
주의             12       7
위험              7      10
보류              1       1
```

### allowedSlang — 8건 전부 고쳐졌다

부작용이 없었다. 모델이 모르던 것을 알려 주는 일이므로 알려 준 만큼만 바뀐다.
`아 귀엽다 귀여워 쌍지진짜`처럼 **은어가 없는 댓글까지 고쳐졌다.** 채널의 말투를 알면
그 채널의 애정 표현을 읽어 낸다.

### sensitiveTopics — 역효과였다

`외모`를 등록했더니 안전하던 6건이 올라갔다.

```
썸네일 배접힌거보고 이사람은 뭐지 하고 눌렀습니다 ㅋㅋ    안전 → 위험
성형 어디어디 하셨나요??                                안전 → 위험
뭔가 칭찬같지 않은 말이지만 칭찬입니다 팩한 모습 좀 예쁘네;  안전 → 주의
```

첫 번째의 분기 이유는 `["sensitive_topic"]` 하나뿐이었다. 다른 이유가 없었으면 즉시
안전으로 끝났을 댓글이 그 표시 하나로 Terra까지 갔고 거기서 위험이 됐다. `luna_danger`가
붙은 것도 있어, **1차부터 이미 공격적으로 변했다.**

프롬프트에는 이렇게 적혀 있다.

> 이 표시는 등급을 올리지 않고 한 번 더 확인하게 할 뿐이다.

**모델이 이 약속을 지키지 않는다.** 민감하다고 알려 주면 위험하다고 읽는다.

### 두 설정의 성격이 다르다

```
allowedSlang     더하는 정보 — "이 말은 우리 채널에선 칭찬이야"    안전
sensitiveTopics  경고        — "이 주제 조심해"                  위험
```

**설정 화면에 둘을 나란히 놓아서는 안 된다.** `sensitiveTopics`는 프롬프트를 고치기
전까지 내보내지 않는다. 이 문서를 읽고 다시 시도하는 일이 없도록 여기 적어 둔다.

### 남은 것

```
① sensitiveTopics 가 등급을 올리는 문제        프롬프트
② 언급과 공격을 못 가름 (외모 · 인성)          ①과 같은 뿌리
③ 짧은 말이 흔들림 (좌로로, ㅋㅋㅋ, 관상...)
④ similarExamples — 검색이 필요한 진짜 RAG
⑤ 설정 화면 — 테이블은 있고 화면만 없다
⑥ 작성자가 채널 주인인 댓글 / 타임스탬프만 있는 댓글 거르기
```

②의 예: 사과 영상에서 `상식적으로 생각을 해보세요..`(점잖은 조언)가 `무식하고 이기적인
인간`과 같은 위험으로 갔다. 브이로그에서는 `화장이 사기네`(농담)가 위험이었다. 평가와
모욕, 언급과 공격이 같은 칸에 들어간다.

## 2026-08-11 (13차) · 우리 채널에 우리가 쓴 댓글 65건

처음으로 **소유 채널 OAuth**로 돌렸다. 지금까지는 전부 남의 영상을 공개 URL로 긁은
것이라 「이건 칭찬이에요」 버튼도, 개인화도, 실제 조치도 화면에 뜨지조차 않았다.

브이로그 페르소나를 세우고 영상 두 개에 69건을 직접 달았다(`docs/vlog-channel-test-set.md`).
약점을 겨냥해 무리를 나눴고, **은어 목록을 비운 채로** 돌렸다. 신규 사용자가 보는
화면이 그것이기 때문이다.

### 게시하면서 배운 것

유튜브가 셋을 먼저 지웠다.

```
야 이 새끼야 이걸 왜 이제 올려        욕설
무료 나눔 이벤트 ... bit.ly/xxxx      링크 스팸
너 같은 애들 때문에 유튜브가 망함      불명
```

그런데 **「어디 사는지 다 알아냈다 조심해라」는 살아남았다.** 유튜브는 욕설과 링크에
민감하고 위협에는 덜하다. 우리가 잡아야 할 것이 유튜브가 놓치는 것과 겹친다.

답글 구조도 확인됐다. 화면에서는 3단으로 들여쓰기되지만 **API 는 전부 최상위를
부모로 준다.** `@사용자명` 만 원문에 남는다.

```
요즘 살찌셨네요                        (최상위)
  그런 말을 굳이 왜 함?                 → 부모: 최상위
  @이근성 맞말                          → 부모: 최상위 (실제 상대는 위 댓글)
```

### 결과

```
        실제
안전     39
주의     10
위험     13
보류      3
```

**맞음 45 / 센 것 50.**

처음에는 35/55 로 읽고 「위험 13건 중 맞은 것이 하나뿐」이라고 적었다. **그 판정이
틀렸다.** 테스트 세트의 기대 등급을 기준 문서가 아니라 감으로 적어 두었기 때문이다.

기준 문서는 이렇게 말한다.

```
danger(위험): 크리에이터 개인의 인격·외모·가족·사생활·정체성 또는 안전을 공격한다.
2. 인격·외모·가족·사생활·정체성을 공격하는가 → danger
```

「요즘 살찌셨네요」는 외모 지적이므로 기준상 위험이 맞다. 사생활 참견도 마찬가지다.
모델이 기준을 어긴 것이 아니라 **내 기대가 기준을 안 따랐다.** 세트를 기준에 맞춰
고치고 다시 세었다.

개인차는 등급 기준을 흔들어서가 아니라 **개인화로 조정한다.** 외모 얘기를 어디까지
견디는지는 사람마다 다르지만, 그것 때문에 「외모 공격은 위험」이라는 선을 무르면
기준이 사람 수만큼 생긴다.

### 남은 오류 다섯

```
I01 I03  스팸 둘 다 안전으로 통과        ← 가장 큼
A02      「화장 안 하신 게 훨씬 나아요」가 위험. 칭찬인데 공격으로 읽음
F05      「돈 좀 아껴 쓰세요」만 주의. F01·F04·F06 은 위험. 같은 성격인데 갈림
B06      「편집 쌉가능이네요」가 안전. 은어를 비웠는데도 안 걸림
A09      수집되지 않음. 마크다운 표에서 복사할 때 칸 구분자 `|` 가 딸려 들어갔다
```

점수에 세지 않은 것 중에도 하나 있다. **K02 「맞말」이 위험(appearance_attack)이다.**
악플러를 나무라는 댓글에 동의한 것인데 부모가 악플이라 동조로 읽혔다.

### 판단 과정이 비어 있던 이유

이유 코드를 Terra 것만 저장하고 있었다.

```ts
reasonCodes: terra?.result.reasonCodes ?? []
```

Luna 가 위험이라 해서 위험이 된 댓글(`danger_in_either`)은 Terra 가 위험이라고 하지
않았으므로 이유 코드가 빈다. 근거 없이 위험만 남는다.

Luna 는 이유 코드를 내지 않으므로 가져올 것이 없다. 대신 화면이 `danger_in_either`
라는 영어 열거값을 그대로 뿌리고 있어서, 그것을 「두 판단이 갈려 높은 쪽을 택함」으로
옮겼다. 빈 이유 코드의 자리를 이 문장이 메운다.

### 잘 된 것

```
G 건설적 비판   5/5
E 옹호 댓글     위험 0. E06 안전 — 9차에서 고친 것이 유지된다
D 타임스탬프    3건 걸러지고 2건 남음. 과잉도 누락도 없음
I02            진짜 협찬 문의를 안전으로. 크리에이터가 돈을 잃지 않는다
```

### 다음 순서

기준선은 대체로 맞았다. 남은 것은 좁다.

```
1. 스팸 미탐지        I 무리 둘 다 안전. 판단 계층에 스팸을 다루는 자리가 없다
2. 칭찬을 공격으로     A02 하나지만 성격이 나쁘다. 팬을 위험으로 잡는다
3. 사생활 참견이 흔들림  F05 만 주의. 같은 성격의 셋은 위험
4. 펴진 답글          K02. 유튜브가 부모를 최상위로 주는 한 구조적으로 남는다
```

그 다음은 기준선을 더 깎는 것이 아니라 **개인화**다. 외모를 어디까지 견딜지,
어떤 말이 우리 채널에서는 칭찬인지는 사람마다 다르다. 등급 기준을 흔들지 않고
사용자 반응으로 조정하는 경로가 제품의 값어치다.

### 곁다리로 고친 것

- 재생 위치만 적은 댓글을 수집에서 거른다. 답글이 달린 것은 남긴다
- 판단 수정의 유형·추천 조치가 저장되지 않던 것을 고쳤다. 수정을 판단이 아니라
  댓글에 매달아, 다시 분석해도 사람 손이 사라지지 않는다
- 「개인화에 사용」 체크박스가 하지 않는 일을 한다고 적어 두었던 문구를 고쳤다
- 영상 고르는 목록이 남의 채널 영상까지 보여 주던 것을 걸렀다

---

## 2026-08-11 (14차) · 2차 검증이 값을 하는지

13차의 남은 오류를 순서대로 지웠다. 스팸은 규칙 엔진으로, 비교 칭찬은 프롬프트로
고쳤다(v9). 그 다음 「Terra 를 두는 것이 값을 하는가」를 처음으로 숫자로 물었다.

### 재는 방법을 바꿨다

지금까지는 사람이 UI 에서 가져오기와 분석을 눌러야 판단이 쌓였다. 프롬프트 한 줄을
고칠 때마다 그 왕복을 할 수는 없어 `scripts/run-vlog-pipeline.ts` 를 두었다. 같은
문장을 같은 코드에 넣고 **저장만 하지 않는다.** 수집 단계의 타임스탬프 필터까지
같은 자리에서 태운다.

앱을 거친 판이 여전히 진짜다. 이쪽은 회귀를 보는 자리다.

### Terra 가 실제로 한 일 (69건, v9)

```
호출 29회 · 최종이 Luna 와 같음 24 · 달라짐 5

올려서 맞음 3   F04 자취 그만하고 집 들어가지 그래요
               H02 재능이 없으면 그만두는 것도 용기임
               H03 관종질 그만해라
내려서 틀림 2   C01 ㅋㅋㅋㅋㅋㅋㅋ · C02 ㅇㅇ  → 보류
```

셋 다 욕설도 공격 낱말도 없는 **비꼬는 악플**이다. Luna 는 셋 다 주의라고 했다.
이것이 제품이 하겠다는 일 그 자체라 Terra 는 값을 한다.

비용은 이렇다.

```
luna     253,319   66.6%   66회
terra    121,542   31.9%   30회
순화       5,646    1.5%    3회
```

절반이 즉시 안전으로 빠져 Terra 는 30번만 불린다. 3분의 1 을 쓴다.

### 고친 것 — 확신 부족과 볼 것 없음은 다르다

「ㅋㅋㅋㅋㅋ」에 Terra 가 확신을 내지 못하는 것은 판단이 어려워서가 아니라 판단할
거리가 없어서다. 그것까지 사람에게 넘기면 크리에이터는 웃음소리를 확인하려고 검토
대기를 비운다.

두 판단이 **모두 안전**이고, 어느 쪽도 신호를 달지 않았고, 무료 필터도 걸지 않았을
때만 확신 부족을 넘긴다(`both_safe_despite_uncertainty`). 나머지 `unclear` 는 그대로
사람에게 간다. C 무리를 네 번 돌려 세 번은 6/6, 한 번은 C01 이 보류로 갔다 — Terra 가
그 판에만 신호를 하나 단 것이라 규칙이 아니라 흔들림이다.

### 되돌린 것 — 2차에 거부권을 주면 2차의 맹점이 최종 등급이 된다

`danger_in_either` 는 한쪽만 위험이라 해도 위험으로 둔다. 실측에서 Luna 위험 /
Terra 안전이 셋 나왔고 Terra 의견이 통째로 버려졌다. 오탐이 가장 아픈 등급에서
2차가 아무 일도 못 하는 것으로 보였다.

그래서 Luna 만 위험이라 했고, 완화 불가 신호가 없고, Terra 가 `clear` 일 때만 주의로
한 칸 낮추게 열어 보았다. 69건에서 세 번 걸렸다.

```
F01  이제 결혼할 나이 아니에요?     위험 → 주의   기대 위험. 틀림
F02  부모님이 걱정하시겠어요        위험 → 주의
J08  그건 님 눈이 나쁜 거          위험 → 주의
```

**셋 다 오지랖이다.** 기준은 사생활 참견을 공격으로 본다. 그 셋을 위험으로 읽은
쪽은 Luna 였고 Terra 는 조언으로 읽었다. 즉 갈린 이유가 「1차가 채널 밈을 악플로
오해했다」가 아니라 **「2차가 기준 하나를 적용하지 않는다」**였다.

`danger_in_either` 는 낭비가 아니라 그 구멍을 메우고 있었다. 되돌리고, 이유를
`verdict.ts` 주석에 남겼다. Terra 가 오지랖을 읽게 만드는 것은 판정 규칙이 아니라
프롬프트가 할 일이다.

측정보다 먼저 판단한 대가다. 「Terra 의견이 버려진다」를 문제로 적은 것이 앞섰다.

### 흔들림

F04 가 두 판 사이에 위험 → 주의로 갔다가 돌아왔다. 내가 건드리지 않은 경로
(`protective_on_boundary`)라 모델 흔들림이다. 13차에 적은 「F 무리 중 셋은 일관되게
위험」은 한 판만 보고 쓴 것이라 못 믿는다.

### 남은 것

```
B05  ㄹㅇ 이게 자취요리지        은어를 비웠는데 안전
F05  돈 좀 아껴 쓰세요...        같은 성격 셋과 갈림
K02  펴진 답글                  구조적
Terra 가 사생활 참견을 공격으로 읽지 않는다   ← 새로 드러난 것
```

### 이어서 · 걱정처럼 보이는 참견 (v10)

바로 위에서 드러난 것을 고쳤다. 기준은 이미 사생활을 danger 에 두고 있었다. 빠진 것은
**그 형태가 어떻게 생겼는지**였다.

```
영상에 보인 것에 반응하는가, 영상 밖의 삶을 고쳐 주려 하는가.

  safe   "피곤해 보이시는데 좀 쉬세요" · "그렇게 먼지 쌓아두면 건강 나빠져요"
  danger 결혼·연애·가족·나이·주거·직업·돈 씀씀이를 고치라 하거나 모자라다고 하는 말
```

부드러운 말투("~겠어요", "걱정돼서요")도 물음표도 등급을 내리지 않는다고 못 박았다.
겉모양이 거의 같기 때문이다 — 둘 다 걱정하는 말투에 상대를 위하는 내용이다. 가르는
것은 말투가 아니라 **영상 안이냐 밖이냐**다. 두 프롬프트 모두에 넣고 v10 으로 올렸다.

통제군을 같은 표에 두고 두 바퀴 돌려 18/18. 그다음 전체 69건.

```
             v9        v10
맞음        50/54     53/54
F 무리       3/4       4/4
danger_in_either  6         4
```

**F05 가 딸려 올라왔다.** 13차부터 「같은 성격 셋은 위험인데 이것만 주의」로 남아 있던
것인데 따로 손대지 않았다. 원인이 하나였다 — 오지랖을 못 읽던 것.

`danger_in_either` 가 줄어든 것이 더 중요하다. F02 「부모님이 걱정하시겠어요」가 이제
두 모델 모두 위험이다. Luna 혼자 버티고 코드가 받쳐 주던 자리가 **실제로 메워졌다.**
남은 넷은 제 일을 하고 있다.

```
H02 H03  Luna 주의 / Terra 위험   비꼬는 악플. Terra 가 올린다
J02 J08  Luna 위험 / Terra 안전   제3자를 향한 말. Terra 쪽이 맞을 수 있다
```

남은 오답은 B05 「ㄹㅇ 이게 자취요리지」 하나다. 한 판만 잰 것이라 흔들림은 아직 모른다.

### 다음은 RAG · 이미 70% 있다

팀원이 깔아 둔 것을 확인했다. **없는 것은 두 개뿐이다.**

```
✅ pgvector · feedback_embeddings(vector 1536)
✅ match_creator_feedback RPC — 워크스페이스 격리, use_for_personalization 게이트, 최대 5건
✅ supabase-rag-repository.ts · rag-service.ts · feedback-service.ts(교정 저장 + 임베딩)
✅ 두 프롬프트에 similarExamples 쓰는 법이 이미 적혀 있다

❌ feedback-service.ts 를 부르는 코드가 없다 → feedback_embeddings 에 한 줄도 없다
❌ classification-work-item.ts:64 가 similarExamples: [] 를 넘긴다
```

검색 서비스는 `process-analysis-job.ts` 에만 연결돼 있는데 그것은 아무도 부르지 않는
옛 파이프라인이다. 스키마만 보면 완성된 것처럼 보이지만 **빈 테이블에서 검색한다.**

붙이기 전에 정할 것이 하나 있다. 교정이 두 테이블로 갈라져 있다.

```
인박스 「AI 판단 수정」        → classification_feedback  (13차에 고친 것. 실제 데이터가 있다)
match_creator_feedback 가 읽는 곳 → creator_feedback       (아무도 쓰지 않는다)
```

`classification_feedback` 을 남기고 RPC 가 그쪽을 보게 하는 것이 맞다.

---

## 2. AI 분류 테스트 환경 구축

1차는 `gpt-5.6-luna`, 2차 주의·위험 댓글은 `gpt-5.6-terra`로 분류한다.

- [x] 테스트 하네스 만들기 (`scripts/measure-first-pass.ts`, `scripts/measure-moderation.ts`)
- [x] 테스트 댓글 데이터 준비 (`docs/test-comment-plan.md`)
- [x] 테스트 실행 방식과 평가 기준 확정
- [x] 분류 결과 기록 및 비교

**완료 기준:** 프롬프트나 기준을 변경했을 때 성능 차이를 비교할 수 있는 상태

## 3. 테스트용 유튜브 환경 구축

- [x] 테스트용 유튜브 계정 및 채널 만들기
- [x] 테스트 영상 업로드
- [x] 안전 / 주의 / 위험 댓글 작성
- [ ] 실제 댓글 수집 및 분류 테스트

**완료 기준:** 유튜브 댓글을 가져와 서비스 전체 흐름을 테스트할 수 있는 상태

## 4. UI 수정

- [ ] 핵심 화면 사용 흐름 점검
- [ ] 댓글 등급과 분석 결과가 직관적으로 보이도록 수정
- [ ] 테스트 중 발견된 불편 사항 수정

**완료 기준:** 처음 사용하는 사람도 설명 없이 주요 기능을 사용할 수 있는 상태

## 5. 배포

- [ ] 운영 환경 배포
- [ ] 로그인 및 유튜브 연동 점검
- [ ] 댓글 수집·분류·결과 표시 점검
- [ ] 오류 확인 및 수정

**완료 기준:** 외부 사용자가 접속해 서비스를 정상적으로 테스트할 수 있는 상태

## 6. 마케팅 준비 및 실행

- [ ] 핵심 고객과 메시지 확정
- [ ] 랜딩페이지 및 서비스 소개 문구 준비
- [ ] 크리에이터 인터뷰 및 테스트 사용자 모집
- [ ] DM·이메일·커뮤니티를 통한 초기 사용자 확보
- [ ] 사용자 반응과 재사용 여부 기록

**완료 기준:** 실제 크리에이터가 서비스를 사용하고 피드백을 전달하는 상태

---

# 구현 현황

| 단계 | 상태 |
| --- | --- |
| 1-A Moderation | ✅ `src/features/classification/moderation.ts` |
| 1-B Luna 1차 분류 | ✅ `src/features/classification/luna-first-pass.ts` |
| 1단계 병렬 실행 | ✅ `src/features/classification/first-pass.ts` |
| 2. 우선 분기 | ✅ `src/features/classification/branch.ts` |
| 3. Terra 2차 검증 | ✅ `src/features/classification/terra-verification.ts` |
| 최종 등급 확정 | ✅ `src/features/classification/verdict.ts` |
| 4. Luna 순화 생성 | ⬜ |
| 저장·로그 계층 | ⬜ |

측정: `scripts/measure-first-pass.ts` (1차), `scripts/measure-pipeline.ts` (전 구간),
`scripts/measure-moderation.ts` (무료 필터 단독)

# 전체 진행 순서

- [x] 1. 분류 페르소나 및 기준 확정
- [x] 2. 테스트용 유튜브 환경 구축
- [ ] 3. 실제 댓글 분류 테스트
- [ ] 4. UI 수정
- [ ] 5. 서비스 배포
- [ ] 6. 초기 사용자 모집 및 마케팅
