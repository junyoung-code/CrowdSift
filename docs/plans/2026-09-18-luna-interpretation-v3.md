# Luna 의미 해석 상세화 — 구현 및 1회 실험 결과

## 구현 체크리스트

- [x] context-v3 / semantic-output-v2: 단일 의미 분석 응답에 대화 상대·발화 행위·명시 의미·함의·부족한 문맥 추가
- [x] comment / parent / title별 근거 검증, 부모 없는 부모 작성자 지정 차단
- [x] 기존 claim/harm 본문 근거 검사와 잔존 claim 기반 등급 정책 유지
- [x] 핵심 의미 복원 불가만 HOLD, 이유·필요 정보 필수
- [x] v1/v2 기록·설정 식별자 호환, v3 재개 설정 분리
- [x] 운영 provider에서 v3 차단, 로컬 비교에서만 사용; Claude 비교 미실행
- [x] comparison 상세 해석·중립 보류 이유·원래 번호·상호 배타적인 집계 및 필터
- [x] 기존 불일치 21건 + 101번, 총 22건을 Luna로 1회 실행
- [x] 관련 Vitest 79개 통과, 타입 검사, lint, build, diff check 통과
- [x] 실제 HTML 집계·필터·원문 접힘·원래 등급 보존 검사 및 브라우저 표시 확인

## 실행 결과

결과 폴더: `measurements/semantic-review-20260917/luna-context-v3-20260918/`

- 분석 모델 gpt-5.6-luna medium / 재작성 low / 검증 medium.
- 분석 호출 22회, 각 댓글 1회. 재작성·검증 포함 기록된 총 호출 40회.
- 일치 9 + 불일치 4 + 평가 미확정 9 + 분석 실패 0 = 22.
- 확정 등급 13건의 일치율: 기존 Luna v2 8/13(61.5%) → v3 9/13(69.2%).
- 불일치 원래 번호: 42, 56, 86, 101.
- 재작성 통과 5건, 실패 1건(96번). 실패 문장은 표시하지 않으며 CAUTION 유지.
- 입력 73,923 / 출력 20,882 tokens. 저장한 Luna 단가 기준 추정 $0.039843. 같은 사례 v2 $0.0281542 대비 약 41.5% 증가. 실제 청구액 아님.
- 중앙 지연 8.961초(v2 6.219초), P95 26.033초(v2 25.299초).
- 기존 사람 등급과 입력 문맥은 수정하지 않았으며 사람의 등급·교정 이유는 모델 입력에 넣지 않았다. 추가 영상 정보도 제공하지 않았다.

## 의미 해석 검토 — 에이전트 관찰, 사람 검수 정답 아님

| 번호 | 결과 | 해석 관찰 |
|---|---|---|
| 42 | SAFE, 불일치 | 부모 댓글에 동조하는 비교는 추출했지만 비하의 함의는 추출하지 못함. v2와 같은 등급 |
| 56 | SAFE, 불일치 | 기존 저장 등급 CAUTION 유지. 사용자도 애매하다고 한 사례이므로 확정적인 모델 오류로 단정하지 않음 |
| 66 | HOLD, 일치 | 불완전한 비교 표현과 대상 생략을 복원 불가 이유로 남기고 필요한 정보를 기록함 |
| 70 | SAFE, 등급 일치 | v2의 CAUTION에서 개선됐지만, 여전히 suggestion으로 추출하고 상대는 unknown. ‘이미 달라진 모습을 긍정하며 시청자에게 옹호’라는 사용자 설명과 다르므로 의미 해석 성공으로 세지 않음 |
| 86 | RISK, 불일치 | 부족한 지칭 문맥을 기록했지만 성적 조롱으로 해석. 사실 정정이라는 사용자 설명과 불일치 |
| 101 | RISK, 불일치 | 사람들에 대한 비판과 크리에이터 칭찬은 분리했지만 비판 대상을 특정하지 못함. 타인 대상 공격으로 확정하지 못해 기존 정책에서 RISK 유지 |

구조 구현은 완료했지만 핵심 대상·발화 의도 오류는 남아 있다. 등급이 바뀐 사례는 70번 하나이며 그 사례도 발화 의도는 여전히 다르다. 이 1회 개발용 선택 사례 실험으로 일반 정확도 개선이나 출시 기준 충족을 주장하지 않는다. 모델 결과를 보고 같은 사례를 추가 재실행하거나 프롬프트를 재조정하지 않았다.

브라우저: http://127.0.0.1:8787/luna-context-v3-20260918/comparison.html

재현 설정·입력은 결과 폴더의 `comparison.json`, `input.json`, `baseline-luna.json`, `prices.json`, `presentation.json`, `selection.json`에 보존한다.
